# Mobile Bug Report Log

**Date**: 2026-05-13  
**Starting ID**: HL78  
**Format**: One table row per bug. Fill from top to bottom and keep IDs sequential. Replace the `HL78` instruction row with the first real bug of this pass, then duplicate that completed row structure for `HL79`, `HL80`, and later bugs.

**Allowed values**: Bug Status = Blocked, In Progress, Completed, Confirm solved, Verified By QA, Reported, Need to Discuss, Pending, Deferred, Needs Revision. Issue Type = Bug, Feedback. Platform = Android.

**Authoring rule for AI agents**: Do not leave the Description cell shallow. Each bug row must be written so the development team can reproduce the issue, understand the business impact, see the backend/API truth, and identify the likely failure layer quickly.

| Date | Description & Screenshot / video | Bug Status | Reported By | Issue Type | Platform | Device & OS version | Comments |
|------|----------------------------------|------------|-------------|------------|----------|---------------------|----------|
| May 13, 2026 | **HL78 - Inquiry AI head scan flow is missing background removal and image annotation actions that were part of the previously demoed full scan workflow**<br><br>**Pre-condition**: The Android patient app inquiry creation flow now includes the integrated AI head scan module. In the earlier demoed AI head scan flow, the scan experience also included image-preparation steps for background removal and image annotation before the images were finalized for use.<br><br>**Test steps**:<br>1. Log in to the Android patient app.<br>2. Start a new inquiry.<br>3. Proceed into the AI head scan step within the inquiry creation flow.<br>4. Complete the available scan/image-capture steps and review the actions available on that screen and the next related screens.<br>5. Check whether there is any visible way to remove the background from the captured images.<br>6. Check whether there is any visible way to annotate or mark up the captured images before submission.<br>7. Compare the available in-flow actions with the previously demoed full AI head scan workflow.<br><br>**Test data**: Inquiry creation flow in Android patient app; AI head scan module integrated into inquiry flow. Exact inquiry ID and screenshot/video evidence to be attached during execution pass.<br><br>**Expected result**: If the inquiry flow is using the AI head scan module that was previously demonstrated as a complete workflow, the user should still have access to the same essential image-preparation actions before submission, especially background removal and image annotation. Those actions should be clearly available within the inquiry scan flow, or there should be an explicit replacement entry point if the functionality was intentionally relocated.<br><br>**Actual result**: In the current inquiry creation flow, the AI head scan module is present, but there is no visible place to trigger background removal and no visible place to annotate the images. The inquiry flow therefore exposes only a partial scan experience compared with the previously demoed workflow, and users currently have no in-flow way to perform those image-preparation steps before submitting the inquiry images.<br><br>**Backend/API reference evidence**: No backend responsibility is expected for this missing behavior. For this feature area, backend responsibility is limited to receiving the final image output. The issue being reported here is that the mobile inquiry flow does not expose the client-side/background-removal and annotation steps before the final image is sent.<br><br>**Suspected layer / root cause direction**: Mobile app feature-integration gap or incomplete workflow wiring. The AI head scan entry point was added into inquiry creation, but the downstream background-removal and annotation capabilities from the earlier full flow do not appear to be exposed in the integrated version before final image submission.<br><br>**Screenshot / video evidence**: To be attached. | Reported | Trung | Bug | Android | Android device, OS version TBD | Mobile-only scope. Backend should continue receiving the final image, but the missing trigger points for background removal and annotation appear to be on the app side. |
| May 13, 2026 | **HL79 - Quote checkout payment flow does not expose saved profile cards and always falls back to manual Stripe card-entry sheet**<br><br>**Pre-condition**: Patient account has one or more saved cards in the profile payment-method area. The patient opens the appointment / quote checkout flow for a payable accepted quote and taps the payment CTA from the appointment detail screen.<br><br>**Test steps**:<br>1. Log in to the Android patient app with a patient account that already has saved cards in profile.<br>2. Open an accepted quote / appointment detail that is ready for payment.<br>3. Tap the payment CTA (`Pay Now`).<br>4. Observe the payment bottom sheet and the next Stripe checkout screen that opens.<br>5. Check whether the app first loads the patient’s saved backend payment methods and allows the user to select one of them.<br>6. Check whether the app instead opens the generic Stripe card-entry/payment sheet that requires the user to type card details again.<br><br>**Test data**: Appointment detail screen shown in attached screenshots; visible deposit/payment CTA shows `Pay Now ($15000.00)` against an appointment view with visible treatment dates `1-14 of Jul` and displayed price `$60000`. Exact quote ID and patient payment-method IDs should be attached during execution pass if needed.<br><br>**Expected result**: When the patient already has saved cards in profile, the quote checkout flow should allow the user to choose from those saved cards instead of forcing manual card entry every time. The intended backend-supported integration path is available:<br>1. Mobile loads saved cards via `GET /api/patient/payment-methods`.<br>2. User selects one saved `payment_method_id`.<br>3. Mobile calls `POST /api/booking/pay-deposit-with-saved-card` with `quote_id`, `payment_method_id`, and optional payment-plan fields.<br>4. Backend validates quote ownership and saved-card ownership, creates and confirms the Stripe `PaymentIntent` server-side, and returns success/failure in a single response.<br>5. On success, backend immediately creates the booking from the quote, marks the first installment/deposit as paid, and moves the quote/booking into the post-payment state.<br><br>**Actual result**: The current Android flow does not expose any saved-card selection UI in quote checkout. Tapping `Pay Now` opens a payment modal that then leads into the generic Stripe checkout/payment sheet (`Pay with Link`, `Card`, `Amazon Pay`), which requires the patient to enter payment details again instead of using the saved cards already stored in the patient profile.<br><br>**Backend/API reference evidence**: The backend already exposes the required saved-card endpoints for this flow. `GET /api/patient/payment-methods` returns the patient’s saved methods for selection. `POST /api/booking/pay-deposit-with-saved-card` accepts `quote_id` and saved `payment_method_id`, validates the card belongs to the authenticated patient, charges the saved card server-side, and returns a success response with `booking_id`, `quote_id`, `payment_intent_id`, paid amount, currency, booking status, and masked card label. The Stripe charge path is server-side create+confirm, not client-side card entry: backend uses the saved Stripe payment method ID to create a `PaymentIntent` with `confirm=true`. After success, backend creates the booking directly from the quote and marks payment state accordingly. This means the missing behavior in the current app flow is not the absence of a backend endpoint for saved-card quote payment.<br><br>**Integration note / limitation**: The saved-card endpoint may still reject cards that require additional Stripe authentication (`requires_action` / 3DS) and in that case instruct the client to use the standard Stripe flow. That is a narrower edge case. The primary issue in this report is that the app never offers the saved-card path at all, even before reaching that edge case.<br><br>**Suspected layer / root cause direction**: Mobile app integration gap. The current payment UI appears wired only to the generic `/api/booking/create-payment-intent` + Stripe client-sheet flow, instead of first checking and offering the backend saved-card flow that already exists for quote deposit payment.<br><br>**Screenshot / video evidence**: [https://s.letweb.net/s/dw7nv7](https://s.letweb.net/s/dw7nv7), [https://s.letweb.net/s/gxj90o](https://s.letweb.net/s/gxj90o) | Reported | Trung | Bug | Android | Android device, OS version TBD | Treat this as a mobile checkout integration bug unless the product explicitly decided not to support saved profile cards in quote payment. If the feature is intended, mobile should integrate `GET /api/patient/payment-methods` plus `POST /api/booking/pay-deposit-with-saved-card` before falling back to manual Stripe entry. |
