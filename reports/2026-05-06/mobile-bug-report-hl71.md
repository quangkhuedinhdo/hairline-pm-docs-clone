# Mobile Bug Report Log

**Date**: 2026-05-06  
**Starting ID**: HL71  
**Format**: One table row per bug. Fill from top to bottom and keep IDs sequential. Duplicate the `HL71` template row for `HL72`, `HL73`, and later bugs.

**Allowed values**: Bug Status = Blocked, In Progress, Completed, Confirm solved, Verified By QA, Reported, Need to Discuss, Pending, Deferred, Needs Revision. Issue Type = Bug, Feedback. Platform = Android.

**Authoring rule for AI agents**: Do not leave the Description cell shallow. Each bug row must be written so the development team can reproduce the issue, understand the business impact, see the backend/API truth, and identify the likely failure layer quickly.

| Date | Description & Screenshot / video | Bug Status | Reported By | Issue Type | Platform | Device & OS version | Comments |
|------|----------------------------------|------------|-------------|------------|----------|---------------------|----------|
| May 6, 2026 | **HL71 - Patient confirmed screen exposes hotel/flight booking actions for provider-included travel path**<br><br>**Pre-condition**: Patient account `joachimtrungtuan.work@gmail.com` opens the newly confirmed inquiry `Thin 0505 1` with inquiry ID `f96fd84c-e60d-4723-addc-9026d8b94806` and selected quote ID `46a07656-534c-46ca-99f5-93827ee37fcd`. This quote is currently treated by the backend as `travel_path=provider_included`.<br><br>**Test steps**:<br>1. Log in to the Android patient app with `joachimtrungtuan.work@gmail.com`.<br>2. Open the confirmed case `Thin 0505 1`.<br>3. Observe the `Hotel & flight` section on the confirmed detail screen.<br>4. Tap `Book hotel` and attempt to submit hotel data.<br>5. Tap `Book flight` / travel flow entry and continue into the travel requirement screen.<br>6. Compare the available patient actions with the backend travel path for the same quote.<br><br>**Test data**: Patient code `HPID2512-0014`; inquiry ID `f96fd84c-e60d-4723-addc-9026d8b94806`; quote ID `46a07656-534c-46ca-99f5-93827ee37fcd`; backend provider ID `57a44ac5-f96b-46b3-8db6-12d4782bf08d`.<br><br>**Expected result**: Because the quote is currently encoded as provider-included travel, the patient confirmed screen should not expose patient-side hotel/flight booking actions that are invalid for this quote. The UI should route the patient only through the actions relevant to the active travel contract. If the patient is not allowed to submit travel details for this quote, the blocking should happen at the action availability level, not only after the user fills a form or enters a subflow.<br><br>**Actual result**: The patient confirmed screen still shows `Book hotel` and `Book flight`, which suggests the patient can manage travel for this quote. After entering the hotel form and tapping submit, the backend rejects the action with `Only assigned provider can submit travel details for this quote.` The patient can also enter the travel requirement flow and is shown options such as `Yes — I need to arrange travel` / `No — I am local / no travel needed`, but choosing `No` is rejected with `Cannot mark no travel required when provider includes travel in package.` This creates a misleading flow where the user is invited into actions that the system already knows are not allowed.<br><br>**Backend evidence**: `GET /api/travel/status?quote_id=46a07656-534c-46ca-99f5-93827ee37fcd` returns `travel_path=provider_included`, `booking_responsibility=provider`, `no_travel_required=false`, and `package_inclusions = { flight: false, hotel: true, airport_transport: true }`.<br>`GET /api/travel/status` therefore already tells the client that this is a provider-managed travel path, not a patient-self-booked flow.<br>The backend enforcement matches that contract: `TravelPathService::validateSubmission()` blocks patient travel submission on Path A, and the hotel submission error shown in the app is consistent with that rule: only provider users may submit travel details when `travel_path=provider_included`.<br>The backend also blocks the Path B-only opt-out route for this quote, which matches the second screenshot error and confirms the patient should not have been offered the `Travel Requirement Check` flow in the first place.<br><br>**Suspected layer / root cause direction**: Likely mobile action-gating bug. The patient app is exposing travel actions without first honoring the backend `travel_path` / `booking_responsibility` contract for the quote.<br><br>**Screenshot / video evidence**: [https://s.letweb.net/s/dz6lnm](https://s.letweb.net/s/dz6lnm), [https://s.letweb.net/s/d1q047](https://s.letweb.net/s/d1q047), [https://s.letweb.net/s/g2xo4r](https://s.letweb.net/s/g2xo4r) | Reported | Trung | Bug | Android | Android device, OS version TBD | Patient-side travel actions should be hidden or replaced with a read-only/provider-managed message whenever the quote is already classified as `provider_included`. |
| May 6, 2026 | **HL72 - Patient app keeps exposing `No travel needed` and outbound-flight submission actions after travel has already been submitted for a confirmed Path B case**<br><br>**Pre-condition**: Patient account `joachimtrungtuan.work@gmail.com` opens confirmed inquiry `Thin 0505 2` with inquiry ID `52e5b604-6b23-4ab4-88f5-5d513380a662` and selected quote ID `b88d976f-b749-43c1-b1b9-265c85ae331b`. The patient has already submitted hotel details and an outbound flight for this Path B case. After that, the patient re-enters the travel requirement screen and taps `No — I am local / no travel needed`.<br><br>**Test steps**:<br>1. Log in to the Android patient app with `joachimtrungtuan.work@gmail.com`.<br>2. Open confirmed case `Thin 0505 2`.<br>3. Submit hotel details and outbound flight details successfully.<br>4. Re-open the travel requirement screen.<br>5. Observe that the app still shows the green `Outbound Flight` action and the red `No — I am local / no travel needed` action.<br>6. Tap the red action and observe the resulting toast behavior.<br>7. Tap the outbound-flight action again and compare the form behavior with the actual backend record state.<br><br>**Test data**: Patient code `HPID2512-0014`; inquiry ID `52e5b604-6b23-4ab4-88f5-5d513380a662`; quote ID `b88d976f-b749-43c1-b1b9-265c85ae331b`; booking ID `79c1d997-f51e-4162-9c1b-40c5d20877d7`; outbound flight ID `76eec2ae-7bb5-4318-8922-1f4a229a6786`; hotel ID `b3e0019b-c612-49f3-874e-e5a7f5ec21a2`.<br><br>**Expected result**: Once an outbound flight and hotel have already been submitted for a Path B confirmed quote, the patient app should present a consistent follow-up state. Existing submitted records should open in read-only or correction mode, and mutually exclusive actions such as `No travel needed` should not still be exposed in the same way. If the case is already marked as `no travel required`, the UI should also not keep showing an outbound-flight submission CTA for that same quote. If no return flight has been submitted yet, the patient app should clearly show that only the return leg is still missing.<br><br>**Actual result**: The patient app continues to expose the red `No — I am local / no travel needed` action even after travel records have already been submitted for the quote. After the red action has already been used, later taps show the toast `Already marked as no travel required`, which means the app is still surfacing that action even though it is no longer a meaningful next step. At the same time, the app continues to expose the `Outbound Flight` action instead of routing the user to a read-only or correction state for the already submitted outbound record. Tapping back into the outbound flow leads to a duplicate-submission error (`Flight information for outbound leg already exists`) instead of opening the existing outbound record directly.<br><br>**Backend/API reference evidence**: `GET /api/travel/status?quote_id=b88d976f-b749-43c1-b1b9-265c85ae331b` returns `travel_path=patient_self_booked`, `booking_responsibility=patient`, `no_travel_required=true`, `outbound_flight.status=submitted`, `hotel.status=submitted`, and `return_flight.status=missing_return_flight` for the same quote.<br>`GET /api/quote/flight-detail?quote_id=b88d976f-b749-43c1-b1b9-265c85ae331b` returns a real outbound record under `data.outbound` and `data.return = null`.<br>`GET /api/quote/flight-detail?quote_id=b88d976f-b749-43c1-b1b9-265c85ae331b&leg_type=return` returns `Flight information not found`, confirming that only the outbound leg exists today.<br>These backend responses are included only as reference evidence for what state the mobile app should now be presenting to the patient.<br><br>**Suspected layer / root cause direction**: Mobile app state/action-gating bug. The patient app is not adapting its travel CTA state correctly after the quote already contains submitted travel data and has been marked as `no travel required` in backend. It continues to expose stale or conflicting next-step actions instead of routing the patient to the correct read-only, correction, or missing-return-flight state.<br><br>**Screenshot / video evidence**: [https://s.letweb.net/s/d7k3oj](https://s.letweb.net/s/d7k3oj), [https://s.letweb.net/s/g8ylp2](https://s.letweb.net/s/g8ylp2), [https://s.letweb.net/s/e9y1qo](https://s.letweb.net/s/e9y1qo) | Reported | Trung | Bug | Android | Android device, OS version TBD | Mobile scope only. Backend/API data is included as reference evidence for the state the app should render, not as the requested implementation scope for this bug ticket. |
| May 6, 2026 | **HL73 - In-progress travel itinerary shows `Not provided` for fields the patient was required to fill, so the saved itinerary does not reflect the data-entry experience in the mobile app**<br><br>**Pre-condition**: Patient account `joachimtrungtuan.work@gmail.com` opens the in-progress case `Thin 0505 2` with inquiry ID `52e5b604-6b23-4ab4-88f5-5d513380a662` and quote ID `b88d976f-b749-43c1-b1b9-265c85ae331b`. During the travel submission flow, the patient is required by the app to enter the relevant fields and is not allowed to leave them blank.<br><br>**Test steps**:<br>1. Log in to the Android patient app with `joachimtrungtuan.work@gmail.com`.<br>2. Open the in-progress case `Thin 0505 2` and go to the travel itinerary view.<br>3. Inspect the outbound flight and hotel detail sections.<br>4. Compare the fields rendered as `Not provided` in the app against the live backend payload for the same quote.<br>5. Cross-check whether those values are present as saved values or returned as `null` in the API response.<br><br>**Test data**: Patient code `HPID2512-0014`; inquiry ID `52e5b604-6b23-4ab4-88f5-5d513380a662`; quote ID `b88d976f-b749-43c1-b1b9-265c85ae331b`; outbound flight ID `76eec2ae-7bb5-4318-8922-1f4a229a6786`; hotel ID `b3e0019b-c612-49f3-874e-e5a7f5ec21a2`.<br><br>**Expected result**: If the app requires the patient to fill a field before submission, the subsequent itinerary view should show the saved value consistently. The app should not later present that same field as `Not provided` unless the patient truly left it empty and the backend accepted that intentionally.<br><br>**Actual result**: In the in-progress itinerary, the app shows some fields as `Not provided` even though the patient had to enter data during the submission flow and could not leave required fields blank. The rendered details therefore do not match the patient’s data-entry experience. This creates a confusing UX/UI state where the user believes they submitted complete information, but the itinerary later presents parts of that information as absent.<br><br>**Backend/API reference evidence**: `GET /api/travel/itinerary?quote_id=b88d976f-b749-43c1-b1b9-265c85ae331b` shows that some fields are returned explicitly as `null`, not as saved values: for hotel, `amenities=null`, `transportation=null`, and `special_request=null`; for outbound flight, `special_request=null`.<br>These backend responses are included only as reference evidence for why the saved itinerary is inconsistent with the mobile submission experience.<br><br>**Suspected layer / root cause direction**: Mobile UX/UI state-consistency issue around travel submission and itinerary rendering. The patient app requires input during submission, but the later itinerary view still shows `Not provided` states instead of the values the patient expects to see, which makes the saved record feel unreliable and confusing from the user’s perspective.<br><br>**Screenshot / video evidence**: [https://s.letweb.net/s/g0omv0](https://s.letweb.net/s/g0omv0), [https://s.letweb.net/s/ejnl13](https://s.letweb.net/s/ejnl13), [https://s.letweb.net/s/dkmo15](https://s.letweb.net/s/dkmo15), [https://s.letweb.net/s/gl9r37](https://s.letweb.net/s/gl9r37) | Reported | Trung | Bug | Android | Android device, OS version TBD | Mobile scope only. Backend/API data is included as reference evidence for what was actually persisted, not as the requested implementation scope for this bug ticket. |
| May 6, 2026 | **HL74 - Help Center app content does not consistently reflect the backend Help Center dataset and counters**<br><br>**Pre-condition**: Patient account `joachimtrungtuan.work@gmail.com` opens the patient Help Center in the Android app and navigates through FAQs, Articles, Resources, and Videos.<br><br>**Test steps**:<br>1. Log in to the Android patient app with `joachimtrungtuan.work@gmail.com`.<br>2. Open Help Center and observe the top content cards and item counters.<br>3. Open FAQs and compare the visible topics, duplicated questions, subtitle, and sample answers against the backend FAQ payload.<br>4. Open Articles and compare the headline titles on the home screen and article list against the backend featured/article endpoints.<br>5. Open Resources and compare the displayed file names against the backend Help Center resource-library dataset.<br>6. Open Videos and compare the app empty-state against the backend video-tutorials dataset.<br><br>**Test data**: Patient account `joachimtrungtuan.work@gmail.com`; backend Help Center endpoints checked on May 6, 2026: `/api/help-centre/overview`, `/api/help-centre/faqs`, `/api/help-centre/patient-articles`, `/api/help-centre/patient-articles/featured`, `/api/help-centre/resource-library`, `/api/help-centre/video-tutorials`.<br><br>**Expected result**: The patient Help Center should reflect the current backend content set and counts consistently across content types. If the backend already has FAQ, article, resource, and video data, the app should surface those datasets faithfully instead of showing mismatched counters, UUID-style placeholders, or empty states that imply the content is unavailable.<br><br>**Actual result**: The app appears to use real backend content for some surfaces but not others. FAQ topics, duplicated FAQ questions, and the lorem-ipsum subtitle match the backend FAQ payload. The `Most Popular Articles` / article titles also match the backend featured articles. However, the content counters shown on the Help Center cards do not match live backend totals, the Resources screen shows UUID-like file names instead of the named resources returned by the Help Center resource-library endpoint, and the Videos card shows `No content available` even though the backend currently returns published video tutorial data. This creates an inconsistent Help Center experience where some screens appear backend-backed and others appear stale, mis-mapped, or placeholder-driven.<br><br>**Backend/API reference evidence**: `GET /api/help-centre/faqs` returns 24 FAQ items across 4 topics and matches the visible FAQ topics/questions, including the lorem-ipsum subtitle and duplicate seeded FAQ entries.<br>`GET /api/help-centre/patient-articles/featured` returns article titles that match the app’s visible articles such as `How to Create Your Patient Account`, `Navigating the Patient Dashboard`, `Understanding the Treatment Journey`, `Comparing and Accepting Quotes`, and `FUE Hair Transplant Explained`.<br>`GET /api/help-centre/patient-articles` returns 10 published guides across 8 categories.<br>`GET /api/help-centre/resource-library` returns 8 named resources such as `Patient Preparation Checklist`, `Aftercare Instructions`, and `Platform User Guide`, which do not match the UUID-style file names shown in the app resources list.<br>`GET /api/help-centre/video-tutorials` returns 6 published videos across 2 categories, so the app’s `No content available` video state does not match the current backend dataset.<br>Live backend totals on May 6, 2026 were: FAQs `24`, Articles `10`, Resources `8`, Videos `6`.<br><br>**Suspected layer / root cause direction**: Mobile app Help Center data-source / mapping inconsistency. Some Help Center surfaces are clearly rendering real backend content, while others appear to use stale counts, wrong fields, or the wrong endpoint/source for resources and videos.<br><br>**Screenshot / video evidence**: [https://s.letweb.net/s/emoz99](https://s.letweb.net/s/emoz99), [https://s.letweb.net/s/ejnl19](https://s.letweb.net/s/ejnl19), [https://s.letweb.net/s/dkmo17](https://s.letweb.net/s/dkmo17), [https://s.letweb.net/s/gl9r3m](https://s.letweb.net/s/gl9r3m), [https://s.letweb.net/s/emoz98](https://s.letweb.net/s/emoz98), [https://s.letweb.net/s/dn5r1j](https://s.letweb.net/s/dn5r1j) | Reported | Trung | Bug | Android | Android device, OS version TBD | Mobile scope only. Backend/API data is included as reference evidence to show that content already exists server-side and to identify which app surfaces are mismatched. |
| May 6, 2026 | **HL75 - Patient support-ticket reply error message is too generic and does not tell the user that replies must be at least 10 characters**<br><br>**Pre-condition**: Patient account `joachimtrungtuan.work@gmail.com` opens support ticket `CASE-2026-00025` (ticket ID `9ac311fd-447e-4900-ad4b-79bd6a6aed81`) in the Android app after the admin reply is already visible in the communication thread.<br><br>**Test steps**:<br>1. Log in to the Android patient app with `joachimtrungtuan.work@gmail.com`.<br>2. Open support ticket `CASE-2026-00025`.<br>3. Tap `Reply`.<br>4. Enter a short message such as `hdjsbfn`.<br>5. Tap `Send Reply`.<br>6. Compare the app toast with the actual backend reply validation rule.<br><br>**Test data**: Ticket number `CASE-2026-00025`; ticket ID `9ac311fd-447e-4900-ad4b-79bd6a6aed81`; patient ID `04c3bf91-49d9-4a25-8418-03a79c7ea24e`; short app-entered sample message `hdjsbfn`; backend-valid sample message `Patient backend reply test 123`; backend-created reply message ID `4f00feb7-a3ab-4030-a57d-2eb6233e5f24`.<br><br>**Expected result**: When the patient enters a reply shorter than the minimum requirement, the app should clearly explain what is wrong and what the user needs to do next. The validation feedback should tell the user that the reply message must contain at least 10 characters, instead of only returning a generic failure message.<br><br>**Actual result**: The patient app shows only the generic toast `Validation failed` after the user submits the short reply `hdjsbfn`. The toast does not explain that the message is below the 10-character minimum, so the user is left guessing what needs to be corrected before retrying.<br><br>**Backend/API reference evidence**: The patient reply endpoint exists at `POST /api/patients/support-tickets/{id}/reply` and is wired in `routes/api.php` to `PatientSupportTicketController::reply`.<br>The backend validation rule is `message: required|string|min:10|max:2000`, with optional `attachments` up to 5 files.<br>A live API call to `POST /api/patients/support-tickets/9ac311fd-447e-4900-ad4b-79bd6a6aed81/reply` with `{\"message\":\"hdjsbfn\"}` returns HTTP `422` with `message = Validation failed` and `errors.message[0] = The message field must be at least 10 characters.`<br>A second live API call on the same ticket with `{\"message\":\"Patient backend reply test 123\"}` returns success with `message_id = 4f00feb7-a3ab-4030-a57d-2eb6233e5f24`, proving the backend reply flow itself is working.<br><br>**Suspected layer / root cause direction**: Mobile app validation-message handling bug. The app is not surfacing the actionable backend validation detail, so the patient sees a vague error instead of the actual minimum-length requirement.<br><br>**Screenshot / video evidence**: [https://s.letweb.net/s/gxj9zw](https://s.letweb.net/s/gxj9zw) | Reported | Trung | Bug | Android | Android device, OS version TBD | Mobile scope only. Backend/API data is included as reference evidence to show that the real problem is the app’s validation feedback, not missing backend reply support. |
| May 6, 2026 | **HL76 - Support ticket list does not provide a pull-to-refresh interaction for reloading updated ticket statuses**<br><br>**Pre-condition**: Patient account `joachimtrungtuan.work@gmail.com` views the support ticket list in the Android app while ticket `CASE-2026-00025` is later changed from `open` to `in_progress` in backend/admin flow.<br><br>**Test steps**:<br>1. Log in to the Android patient app with `joachimtrungtuan.work@gmail.com`.<br>2. Open the support ticket list and observe the current status shown for `CASE-2026-00025`.<br>3. Leave the app running and change the same ticket status in backend/admin flow from `open` to `in_progress`.<br>4. Return to the support ticket list in the app.<br>5. Try to refresh the list by pulling down on the list screen.<br>6. Observe whether the UI provides a refresh interaction for reloading the latest ticket statuses.<br><br>**Test data**: Ticket number `CASE-2026-00025`; ticket ID `9ac311fd-447e-4900-ad4b-79bd6a6aed81`; patient ID `04c3bf91-49d9-4a25-8418-03a79c7ea24e`.<br><br>**Expected result**: The support ticket list should provide a pull-to-refresh interaction so the patient can manually reload the latest ticket statuses after backend state changes such as a move from `Open` to `In Progress`.<br><br>**Actual result**: The support ticket list does not provide a pull-to-refresh interaction. After the support ticket status changes in backend, the patient has no direct in-screen refresh action to reload the latest ticket state from the list surface.<br><br>**Backend/API reference evidence**: The backend ticket status update succeeds through `PATCH /api/admin/support-cases/9ac311fd-447e-4900-ad4b-79bd6a6aed81` with payload `{\"status\":\"in_progress\"}` and returns `status = in_progress`.<br>`GET /api/patients/support-tickets/9ac311fd-447e-4900-ad4b-79bd6a6aed81` also returns the updated patient-facing ticket state with `status = in_progress`.<br>These backend responses are included only as reference evidence to confirm that the ticket status really changed server-side while the mobile list lacked a manual refresh action.<br><br>**Suspected layer / root cause direction**: Mobile app support-ticket list UX gap. The backend state updates correctly, but the list screen does not offer a pull-to-refresh interaction for the patient to request the latest data.<br><br>**Screenshot / video evidence**: To be attached. | Reported | Trung | Bug | Android | Android device, OS version TBD | Mobile scope only. Backend/API data is included as reference evidence to show that the status change is already persisted server-side and the missing refresh affordance is on the app list surface. |
| May 6, 2026 | **HL77 - Notification items are visible in the patient app but do not deep-link to the related in-app destination when tapped**<br><br>**Pre-condition**: Patient account `joachimtrungtuan.work@gmail.com` has multiple visible notifications in the Android app, including support-ticket updates and quote/travel-related updates.<br><br>**Test steps**:<br>1. Log in to the Android patient app with `joachimtrungtuan.work@gmail.com`.<br>2. Open the `Notifications` screen.<br>3. Tap a support-ticket notification such as `Your support case has been reopened`, `Ticket status changed to open`, `Your support case has been resolved`, or `Your support ticket has been closed`.<br>4. Tap a quote/travel-related notification such as `Flight booking confirmed for quote #b88d976f`, `Hotel booking confirmed for quote #b88d976f`, `Quote price has been updated`, or `Your treatment has started. Quote #e9737d6c is now in progress.`<br>5. Observe whether the app navigates into the relevant support-ticket detail, quote detail, or other specific destination tied to that notification.<br><br>**Test data**: Patient account `joachimtrungtuan.work@gmail.com`; support-ticket notifications tied to `CASE-2026-00025`; quote/travel notifications visible for quote IDs including `b88d976f` and `e9737d6c` as shown in the app list.<br><br>**Expected result**: Tapping a notification item should navigate the patient directly to the relevant in-app destination for that notification. Support-ticket notifications should open the related support ticket detail, and quote/travel notifications should open the relevant quote or treatment detail screen.<br><br>**Actual result**: Notification items are shown in the list, but tapping them does not lead anywhere useful in the app. For support-ticket update notifications, there is no in-app deep-link into the related ticket. The same problem also occurs for quote/travel notifications such as quote-price updates and booking confirmations: tapping the notification does not open the related quote detail or other relevant destination. From the patient perspective, the notifications are visible but non-actionable.<br><br>**Backend/API reference evidence**: Not required for this mobile-only report row at this stage. The issue being reported here is the missing in-app navigation/deep-link behavior from already rendered notification items.<br><br>**Suspected layer / root cause direction**: Mobile app notification interaction / deep-link routing bug. The app renders notification cards but does not wire them to the related destination screens when tapped.<br><br>**Screenshot / video evidence**: [https://s.letweb.net/s/dz6l7l](https://s.letweb.net/s/dz6l7l), [https://s.letweb.net/s/d1q05q](https://s.letweb.net/s/d1q05q) | Reported | Trung | Bug | Android | Android device, OS version TBD | Mobile scope only. This row is about the missing in-app destination behavior from the notifications list, not about backend notification generation itself. |
