# Sprint 1 Readiness & Fix Backlog

---

## Document Control & Sprint Summary

| Field | Value |
|---|---|
| Sprint | Sprint 1 |
| Theme | Core: Inquiry, Quote & Treatment |
| Dates | May 19 (Tue) - May 29 (Fri), 2026; kickoff ceremony May 20 (Wed) |
| Working days | 9 |
| Goal | Complete and verify the core clinical journey from patient registration through inquiry, quoting, booking, and treatment execution across all three tenants simultaneously. Aftercare modules move to Sprint 2 where they are grouped with the configuration and template setup they depend on. |
| Definition of Done | All Sprint 1 modules pass QA on staging; no open critical P0/P1 bugs on any Sprint 1 module; the core commercial journey from registration through inquiry, quote acceptance, booking, and deposit is testable end-to-end on staging; treatment execution is verified against a fully-paid staging booking fixture in Sprint 1 while the patient final-balance path that unlocks real check-in is completed in Sprint 2; Apple Developer Program and Google Play Console accounts are created and verified. |
| Source launch plan | `local-docs/product-plans/2026-05-13/launch-plan.md` |
| Prepared by | Codex AI agent |
| Prepared date | 2026-05-29 |
| Product review date(s) | Not performed in this pass; this report prepares the Sprint 1 scope and readiness backlog from the launch plan. |
| Product environment(s) checked | No staging, production, local build, or app build was checked in this pass. |
| Review scope boundary | Source review only. Scope, DoD, modules, stories, deferrals, and readiness gaps are derived from the Sprint 1 section of the launch plan. Product defects must be added after real staging/product review with evidence links. |

<!--
AI agent guidance: Keep this guidance hidden from Markdown preview. It exists only to preserve the intended workflow for future agents editing this report.

## How To Use This Template

- Section 1 mirrors the launch plan. Do not add new scope, reinterpret the sprint, or silently move work between sprints.
- Section 2 is the working fix backlog. Capture issues found from real product review in enough detail that the dev team can reproduce and fix them quickly. Rows marked as evidence gaps are not confirmed product bugs; they are required product-review checks that must be backed by staging evidence before Sprint 1 can be treated as complete.
- Section 3 is for findings that should not distract the current sprint. Use it to prevent out-of-scope issues from being treated as sprint commitments.
- Do not include Plane ticket IDs, assignees, estimates, or ownership fields. Those belong to the later Plane-ticket creation workflow.

### Priority Scale

| Priority | Meaning |
|---|---|
| P0 | Blocks sprint completion, staging validation, or a core end-to-end journey. |
| P1 | Major required feature gap or broken required flow for this sprint. |
| P2 | Required but contained issue that does not block the whole sprint. |
| P3 | Minor UX, copy, polish, or cleanup issue that should not block sprint completion. |

-->

# 1. Sprint Scope From Launch Plan

## 1.1 Modules In Scope

| Surface | Module | FR / Scope Notes | Launch Plan Reference |
|---|---|---|---|
| Patient Mobile | P-01 Auth & Profile Management | FR-001 Patient Authentication; FR-026 App Settings & Security consumer behavior only. Policy/reason lists are managed in A-09a. | Sprint 1 Modules |
| Patient Mobile | P-02 Quote Request & Management | FR-003 Inquiry Submission; FR-004 Quote Submission; FR-005 Quote Comparison & Acceptance; FR-022 quote comparison filter P1. | Sprint 1 Modules |
| Patient Mobile | P-03a Booking & Payment | FR-006 Booking & Scheduling; FR-007 core deposit/base-currency portion only. Payment sub-features move to Sprint 2. | Sprint 1 Modules |
| Patient Mobile | P-07 Head-Scan Photo Capture & Viewing | FR-002 V1 standardized 2D photo-set capture/viewing; true 3D deferred. | Sprint 1 Modules |
| Provider Web | PR-01 Auth & Team Management | FR-009 Provider Team Roles. | Sprint 1 Modules |
| Provider Web | PR-02 Inquiry, Quote & Booking Management | FR-003 provider-side inquiry handling; FR-004 provider-side quote submission; FR-006 provider-side booking management; includes PR-02b quote withdrawal and confirmed-booking list/detail. | Sprint 1 Modules |
| Provider Web | PR-03 Treatment Execution & Documentation | FR-010 Treatment Execution. | Sprint 1 Modules |
| Provider Web | PR-06 Profile & Settings Management | FR-032 Provider Dashboard Settings; FR-024 Treatment Package Management provider tier. | Sprint 1 Modules |
| Admin Web | A-01 Patient Management & Booking Oversight | FR-016 Admin Patient Management; admin oversight for FR-003, FR-004, FR-005, and FR-006. | Sprint 1 Modules |
| Admin Web | A-02 Provider Management & Onboarding | FR-015 Provider Management. | Sprint 1 Modules |
| Admin Web | A-09a Content & Treatment Management | FR-024 admin treatment/package catalog; FR-025 questionnaire publication; FR-026 configuration owner; FR-027 legal content management. | Sprint 1 Modules |
| Shared Service | S-01 Head Scan Media Processing Service | FR-002 media intake, validation, and alert classification. | Sprint 1 Modules |
| Shared Service | S-02 Payment Processing Service | FR-007 core deposit infrastructure. | Sprint 1 Modules |
| Shared Service | S-05 Media Storage Service | FR-002 encrypted media storage layer; FR-023 retention alignment later in launch. | Sprint 1 Modules |

## 1.2 User Stories In Scope

### Patient

- As a new patient, I want to register with email OTP verification and build my health and preference profile, so clinics can match me with suitable treatments.
- As a patient, I want to browse treatments, complete inquiry details, upload a standardized head-scan photo set, and select preferred providers from the available list before final inquiry submission.
- As a patient, I want to cancel an inquiry and have related open quotes and held slots released.
- As a patient, I want to receive and compare quotes sorted by price, graft count, rating, or date.
- As a patient, I want to accept a quote with its provider-pre-scheduled treatment date, pay a base-currency deposit, and keep the slot only while the payment window is valid.
- As a patient, I want refund eligibility explained before payment without expecting a Sprint 1 refund-management UI.
- As a patient, I want to see treatment status and day-by-day clinical updates after provider check-in.

### Provider

- As a provider team member, I want secure login with role-based permissions and task reassignment when a colleague is suspended.
- As a provider, I want to receive anonymized pre-payment inquiries, review questionnaire/head-scan data, and submit a tailored quote with a pre-scheduled slot.
- As a provider, I want to withdraw a submitted quote with a reason logged.
- As a provider, I want to document treatment day-by-day with status, notes, prescription, actual graft count, and final head scan.
- As a provider, I want to manage clinic profile, languages, staff, awards, reviews display, documents, account settings, notification preferences, billing settings, and package catalog.

### Admin

- As an admin, I want a patient management dashboard with all eight tabs connected to live data.
- As an admin, I want to see stuck inquiries, delayed quotes, zero-quote cases, quote acceptance context, and booking exceptions so I can intervene where the core journey is blocked.
- As an admin, I want to onboard providers through a guided wizard and suspend/deactivate providers with audit logging.
- As an admin, I want to manage inquiry/aftercare/multi-context questionnaire sets with version history and MVP Yes/No constraint enforcement where required.
- As an admin, I want to draft, preview, and publish legal documents with version history and acceptance coverage.
- As an admin, I want to configure OTP behavior, country/calling-code lists, cancellation/deletion reason lists, and OTP email template content.

### Shared Services / Platform Foundations

- As any platform user, I want secure, fast, persistent login across devices.
- As a patient or provider, I want inquiry received, quote ready, and booking confirmed events recorded and visible in the relevant surfaces during Sprint 1, with full push/email/in-app delivery verified later in Sprint 3 under S-03.
- As the platform, I need head-scan media ingested, validated, classified for medical alerts, and stored encrypted with signed-URL access.
- As the platform, I need deposit charges to flow through Stripe with audit logging and refund eligibility computed from shared policy logic.

### Affiliate / Partner

- No affiliate or partner module is assigned to Sprint 1.

## 1.3 Explicitly Deferred / Out Of Scope

| Item | Launch Plan Reason / Destination | Notes |
|---|---|---|
| P-05 Aftercare & Progress Monitoring | Deferred to Sprint 2 | Grouped with aftercare template and configuration setup. |
| PR-04 Aftercare Participation | Deferred to Sprint 2 | Grouped with aftercare template and configuration setup. |
| A-03 Aftercare Team Management | Deferred to Sprint 2 | Grouped with aftercare template and configuration setup. |
| P-03b Payment sub-features | Deferred to Sprint 2 | Installments, multi-currency display, payment confirmation reference, and refund request are not Sprint 1 scope. |
| Patient final-balance path that unlocks real provider check-in | Deferred to Sprint 2 | Sprint 1 verifies treatment execution against a fully-paid staging booking fixture. |
| Patient/provider travel workflows and shared travel gateway | Deferred to Sprint 2 | Patient-side, provider-side, and S-04 travel integration are Sprint 2 scope. |
| Admin embedded travel oversight | Deferred to Sprint 4 | Admin travel oversight is not Sprint 1 scope. |
| Full push/email/in-app notification delivery verification | Deferred to Sprint 3 under S-03 | Sprint 1 records visible product events/states; delivery-channel verification is later. |
| Provider payout overview and admin refund-processing UI | Deferred to PR-05/A-05 where mapped | Not S-02 UI scope in Sprint 1. |
| True V2 3D head-scan viewing | Out of launch scope unless separately approved | Sprint 1 uses V1 standardized 2D photo-set capture/viewing. |
| Affiliate/partner launch surfaces | Later launch scope | FR-018 is launch scope, but no affiliate/partner Sprint 1 module is assigned. |

---

# 2. Sprint Fix Backlog

## 2.1 Sprint-Level Blockers

| Bug ID | Priority | Area | Issue | Steps to Reproduce | Actual Outcome | Expected Outcome | Evidence Link | Task Status | Notes |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
|  | P0 | Sprint QA evidence | Sprint 1 cannot be closed from this report alone because no staging QA evidence is attached for all Sprint 1 modules. | 1. Open this report. 2. Review Document Control and module sections. 3. Check for staging/build evidence links. | No product environment was checked in this pass. | Each Sprint 1 module has staging QA evidence and clear pass/fail status. | TBD | Review pending | Readiness blocker, not a confirmed product defect. |
|  | P0 | Core commercial journey | End-to-end staging proof is still required for registration -> inquiry -> quote acceptance -> booking/deposit. | 1. Run the core journey on staging with patient, provider, and admin access. 2. Capture screenshots/logs for every state transition. | No end-to-end staging evidence is attached in this report. | Journey is testable end-to-end on staging with provider/admin state visibility and audit/payment records. | TBD | Review pending | Direct Sprint 1 DoD gate. |
|  | P1 | Treatment execution fixture | Fully-paid staging booking fixture proof is still required before PR-03 treatment execution can satisfy Sprint 1 DoD. | 1. Create or identify a fully-paid confirmed booking fixture. 2. Provider checks in patient. 3. Provider documents treatment and completes end-of-treatment fields. | Fixture was not checked in this pass. | Treatment execution is verified against a fully-paid staging booking fixture; real patient final-balance path remains Sprint 2. | TBD | Review pending | Prevents overclaiming treatment readiness. |
|  | P1 | App Store account setup | Apple Developer Program and Google Play Console account verification evidence is still required. | 1. Confirm Apple account status. 2. Confirm Google Play Console account status. 3. Attach account-ready evidence. | Account status was not checked in this pass. | Both store accounts are created and verified for the release path. | TBD | Review pending | Non-dev Sprint 1 milestone due May 25 in launch plan. |
|  | P1 | A-09a dependency risk | A-09a questionnaire/catalog/settings readiness is a high-risk Sprint 1 dependency because inquiry testing depends on published active sets and policy/reason configuration. | 1. Review A-09a staging screens/API. 2. Publish active inquiry questionnaire set. 3. Validate patient inquiry consumes active set. | No A-09a product evidence is attached in this report. | Questionnaire catalog, active set publication, legal content, and required settings are ready enough for Sprint 1 inquiry flow QA. | TBD | Review pending | Launch plan risk register explicitly calls out A-09a at 30% completion as high risk. |
|  | P0 | Active inquiry questionnaire source of truth | Scout signal: Admin questionnaire activation, patient inquiry intake, and provider or admin inquiry rendering are still split across different data sources. | 1. Publish or switch the active inquiry set in Admin A-09a.<br>2. Open patient inquiry and inspect the visible questions.<br>3. Submit an inquiry with distinctive answers.<br>4. Review the same inquiry in provider and admin surfaces, including medical alert display. | Admin questionnaire catalog list and create now talk to live questionnaire-set APIs, but detail publish and activate logic remains FE-local in places, while patient inquiry still submits a fixed medical-history map and provider or admin inquiry rendering still reads static backend medical-question config.<br>This creates a concrete risk that the active Admin questionnaire does not control the actual inquiry flow end-to-end. | One authoritative active inquiry questionnaire should drive Admin publication, patient intake, stored inquiry answers, provider or admin inquiry rendering, and medical alert logic end-to-end. | Code review 2026-06-18 | Scout flagged | Cross-tenant dependency touching A-09a, PR-02, and inquiry triage.<br>Prioritize this before broad inquiry regression because it can invalidate downstream QA evidence. |

## 2.2 Module Fix Backlog

## P-01 - Auth & Profile Management

### Review Notes

- Checked areas: Launch-plan scope only.
- Current state: Product behavior not checked in this pass.
- Review limits: Requires staging mobile review for signup, OTP, login, recovery, profile editing, password policy, and account deletion request flow.

### Remaining Fixes

| Bug ID | Priority | Flow / Story | Issue | Steps to Reproduce | Actual Outcome | Expected Outcome | Evidence Link | Task Status | Notes |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
|  | P1 | Patient registration and profile setup | Evidence gap: P-01 Sprint 1 behavior has not been verified in staging for this report. | Run patient signup, email OTP verification, login, password recovery, profile edit, password policy validation, and account deletion request. | Not reviewed in this pass. | All P-01 launch-plan outcomes work on staging. | TBD | Review pending | Add confirmed bugs here after product review. |

## P-02 - Quote Request & Management

### Review Notes

- Checked areas: Launch-plan scope only.
- Current state: Product behavior not checked in this pass.
- Review limits: Requires full patient inquiry, provider selection, quote comparison, quote-specific question, cancellation cascade, and 48-hour payment-window review.

### Remaining Fixes

| Bug ID | Priority | Flow / Story | Issue | Steps to Reproduce | Actual Outcome | Expected Outcome | Evidence Link | Task Status | Notes |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
|  | P0 | Inquiry -> quote acceptance | Evidence gap: the critical patient inquiry and quote-management path has not been verified in staging for this report. | Submit inquiry through all launch-plan steps, select providers, receive quotes, sort/filter comparison, ask quote question, accept quote, and test cancellation cascade where applicable. | Not reviewed in this pass. | Inquiry, provider selection, quote comparison, quote question, acceptance, cancellation cascade, and 48-hour payment-window behavior match Sprint 1 scope. | TBD | Review pending | Core journey blocker until evidence is attached. |

## P-03a - Booking & Payment

### Review Notes

- Checked areas: Launch-plan scope only.
- Current state: Product behavior not checked in this pass.
- Review limits: Requires base-currency deposit payment, released-slot validation, payment-failure hold, and booking confirmation review.

### Remaining Fixes

| Bug ID | Priority | Flow / Story | Issue | Steps to Reproduce | Actual Outcome | Expected Outcome | Evidence Link | Task Status | Notes |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
|  | P0 | Booking and core deposit | Evidence gap: booking and deposit flow has not been verified in staging for this report. | Accept quote with provider-pre-scheduled slot, pay base-currency deposit, test failed-payment retry window, and open confirmation/itinerary view. | Not reviewed in this pass. | Deposit is captured, booking state updates correctly, stale/released slots are blocked, and confirmation/remaining-balance display is visible. | TBD | Review pending | P-03b sub-features remain Sprint 2. |

## P-07 - Head-Scan Photo Capture & Viewing

### Review Notes

- Checked areas: Launch-plan scope only.
- Current state: Product behavior not checked in this pass.
- Review limits: Requires mobile photo-set capture and post-processing viewing review.

### Remaining Fixes

| Bug ID | Priority | Flow / Story | Issue | Steps to Reproduce | Actual Outcome | Expected Outcome | Evidence Link | Task Status | Notes |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
|  | P1 | V1 photo-set contract vs legacy 3D labels | Scout signal: head-scan validation and returned labels still use legacy `3D Head Scan` language even though Sprint 1 scope is standardized 2D photo sets. | 1. Capture the required Sprint 1 photo angles.<br>2. Upload the set and wait for processing.<br>3. Inspect validation errors, returned media labels, and viewing entry points in patient or provider records. | Current inquiry and media code still uses legacy names such as `scan_3d`, `3D Head Scan is required`, and `View 3d head scan` for a flow that Sprint 1 treats as a standardized 2D photo set.<br>This creates a concrete risk of wrong validation, wrong UI wording, or mistaken acceptance criteria during manual QA. | Sprint 1 head-scan flow should consistently behave and present itself as the approved V1 standardized 2D photo-set contract across upload, processing, and viewing. | Code review 2026-06-18 | Scout flagged | Good early manual check for P-07 because it is narrow, user-visible, and likely to expose deeper upload or mapping issues quickly. |
|  | P1 | Head-scan photo set | Evidence gap: V1 photo-set capture/viewing has not been verified in staging for this report. | Capture required 2D photo angles, upload set, wait for S-01 processing, and view returned media from patient record/profile. | Not reviewed in this pass. | Client validation, upload, processing callback, and viewing path work for V1 standardized 2D photo sets. | TBD | Review pending | True 3D viewing is out of launch scope unless separately approved. |

## PR-01 - Auth & Team Management

### Review Notes

- Checked areas: Provider Web `Team` page for an existing onboarded provider; Provider invitation list and invitation email flow; invitation account-setup and first login for invited clinic staff; compared against Admin provider detail `Staff list` for the same provider.
- Current state: Direct provider-dashboard testing has now confirmed several working pieces in the invite flow: invitation creation reaches the `Invitations` table, dashboard `Copy Link` opens the working public invitation page, invited staff can complete account setup, accepted staff can log in, and accepted accounts appear in the Provider `Team` list. After creating four invited staff accounts, the live `Team` table and accepted-invitation list reflect those records, but provider/admin profile-detail surfaces and provider-detail APIs still remain on a separate legacy two-member roster. The remaining major blocker is RBAC containment: invited `Manager`, `Clinical Staff`, and `Billing Staff` accounts are all currently landing in the admin-style dashboard surface instead of a provider-scoped role view.
- Review limits: Detailed provider-tenant permission boundaries for invited `Manager`, `Clinical Staff`, and `Billing Staff` remain blocked because those accounts currently land in the admin-style dashboard surface instead of a valid provider-scoped role view. Proper suspend/remove, task transfer, session revocation, and related lifecycle checks are also still blocked or only partially testable because the dedicated suspend flow is not wired and the generic `Active` / `Inactive` toggle currently leaves account access in an inconsistent state.

### Remaining Fixes

| Bug ID | Priority | Flow / Story | Issue | Steps to Reproduce | Actual Outcome | Expected Outcome | Evidence Link | Task Status | Notes |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| PR-01-001 | P1 | Provider staff roster parity across surfaces | Newly invited staff can be created and appear in Provider `Team`, but provider/admin profile-detail surfaces and provider-detail APIs still show a separate outdated two-member staff roster for the same clinic. | 1. In Provider `Team`, create and accept staff invitations until four invited staff accounts are active.<br>2. Confirm the active `Team` table and accepted `Invitations` list in Provider dashboard.<br>3. Open Provider `Provider Profile` detail and Provider `Edit Profile -> Staff list`.<br>4. Open Admin provider detail `Staff list` and Admin edit-provider `Staff list` for the same clinic.<br>5. Call `provider/get-single-provider?provider_id=e4893814-817a-4565-a41f-795ad0ba609e`, `provider/get-provider-staff?provider_id=e4893814-817a-4565-a41f-795ad0ba609e&page=1&page_size=20`, and `admin/provider-management/providers/e4893814-817a-4565-a41f-795ad0ba609e/staff?per_page=20&page=1`. | Provider `Team` now shows four active invited staff rows (`Olivia Carter`, `Ethan Brooks`, `Sophie Bennett`, `Liam Foster`), and the Provider `Invitations` table shows the same four invitations as `ACCEPTED`.<br>However, Provider `Provider Profile` detail and Provider `Edit Profile -> Staff list` still render only two legacy people: `Dr. Michael Johnson` and `Mehmet Arslan`.<br>Admin provider detail `Staff list` and Admin edit-provider `Staff list` show the same stale two-member roster instead of the four invited staff accounts.<br>Live API check on 2026-06-08 confirms the same mismatch: `get-single-provider`, `get-provider-staff`, and the admin provider-management staff endpoint each return only 2 `provider_users` rows for this provider, matching the stale profile/detail cards rather than the active Provider `Team` table.<br>Code evidence: Provider `Team` reads `team/members` from `ProviderTeamController`, which queries `provider_team_members`, while Provider/Admin profile-detail surfaces and provider staff/detail endpoints still read `provider_users` from `ProviderController`, `ProfileSettingsTab`, `providerDetailsComponents/Staff`, and the admin provider-management staff query path. | Once invited staff accounts are accepted and active, all provider staff surfaces for the same clinic should converge on one consistent roster across Provider `Team`, Provider profile/detail, Provider edit-profile `Staff list`, Admin provider detail `Staff list`, Admin edit-provider `Staff list`, and the supporting API payloads.<br>The same created staff records should not disappear or fork into different staff universes depending on which surface is opened. | https://s.letweb.net/s/dn55p0; https://s.letweb.net/s/govv05; https://s.letweb.net/s/epmmrj; https://s.letweb.net/s/dqvvn9; https://s.letweb.net/s/gryyo4; https://s.letweb.net/s/evqq93 | Task created (HAIRL-1299) | This started as a `Team` visibility mismatch, but follow-up testing on 2026-06-08 shows the newer invite flow can populate `Provider Team` while profile/detail surfaces remain pinned to legacy `provider_users` data.<br>Live API confirmation: `get-single-provider` returns only `test_provider3@clinic.com` and `mehmet.arslan@xhairtransplant.com`; `get-provider-staff` and admin `provider-management/providers/{id}/staff` also return only those two rows. |
| PR-01-002 | P1 | Staff invitation email acceptance link | Provider invitation record is created and visible in `Invitations`, but the email uses the wrong public acceptance URL slug, so both the button and raw fallback link open a 404 path instead of the valid invitation landing page. | 1. In Provider dashboard `Team`, click `Invite Staff` and send an invitation.<br>2. Confirm the pending invitation appears in the `Invitations` table.<br>3. Use `Copy Link` from the Provider dashboard and note the generated path.<br>4. Open the invitation email sent to the invited staff member.<br>5. Compare the raw URL and button target in the email against the copied dashboard link.<br>6. Click the email link. | Pending invitation appears correctly in Provider `Invitations` with actions such as `Resend` and `Copy Link`.<br>The dashboard copy-link uses `/invitation/{id}` and opens the public invitation landing page.<br>The invitation email button and raw fallback link use `/accept-invitation/{id}` instead.<br>Because the frontend route is registered for `/invitation/:invitationId`, clicking the email link leads to a 404/invalid path instead of the intended invitation acceptance flow. | Invitation email button and raw fallback URL should use the same public path as the dashboard `Copy Link` action, so the invited staff member lands on the correct invitation acceptance page and can complete account setup. | https://s.letweb.net/s/g0omnl; https://s.letweb.net/s/ejnlo5 | Task created (HAIRL-1300) | Code evidence: `TeamInvitationMail` defaults to `/accept-invitation/{id}`, while Provider frontend registers `/invitation/:invitationId` and `InvitationTable` copy-link also points to `/invitation/{id}`.<br>This confirms invitation creation works, but email acceptance is broken by URL mismatch. |
| PR-01-003 | P1 | Accepted invitation lifecycle cleanup | After a staff invitation is accepted and the new member already appears in the `Team` list, the same invitation still remains visible in `Invitations` with status `ACCEPTED`. | 1. Send a Provider staff invitation.<br>2. Complete account setup successfully from the invitation flow.<br>3. Return to Provider owner `Team` page.<br>4. Compare the top `Team` list and bottom `Invitations` list for the same email. | The accepted manager account appears in the active `Team` list, but the same email still remains in `Invitations` with status `ACCEPTED`.<br>This keeps a completed operational invitation in the live invitation queue instead of clearing it after success.<br>Code evidence: the invitation list endpoint returns all invitation statuses by default, including `accepted`, and the current UI also includes an `Accepted` status filter. | Once an invitation has been accepted and the user account is active in `Team`, that invitation should disappear from the operational invitation list, or be moved to a separate history/audit view rather than remaining in the same working queue as pending invitations. | https://s.letweb.net/s/gl9rvz | Task created (HAIRL-1301) | This is a direct Provider owner dashboard observation after the first manager account was created successfully on 2026-06-08.<br>Backend `team/invitations` currently returns accepted rows by default, so the current behavior appears intentional in code but is not aligned with the expected operational UX. |
| PR-01-004 | P2 | Invitation account-setup phone input | Invitation landing page uses a different phone-entry pattern from the rest of the provider flows and rejects pasted numbers with spaces too aggressively, making staff account setup inconvenient. | 1. Open a valid Provider staff invitation landing page.<br>2. In `Phone Number`, paste a common formatted number such as `+1 647 555 0101`.<br>3. Compare this field with other provider/admin phone-entry patterns that use separate country-code selector plus number input. | Invitation signup uses one free-text phone field instead of the more common split country-code + phone-number pattern used elsewhere in provider/admin flows.<br>The pasted value `+1 647 555 0101` triggers validation error instead of being normalized.<br>The form asks for strict E.164-style input, which makes copy-paste from normal formatted phone strings inconvenient. | Invitation account-setup should use the same phone-entry pattern as the rest of the provider/admin experience, including country-code selection where that is the established pattern.<br>The field should normalize common separators such as spaces before validation so formatted pasted numbers are accepted. | https://s.letweb.net/s/g0omnl; https://s.letweb.net/s/dkmoqv | Task created (HAIRL-1302) | Screenshot evidence shows the validation failure on the invitation page.<br>Code evidence: invitation `SignupForm` uses a plain `Input` with strict E.164 regex validation, while provider requirements expect worldwide country-code selection for phone flows. This is primarily a UX/consistency issue unless a downstream save failure is observed. |
| PR-01-005 | P3 | Invite staff modal copy typo | Invite staff modal placeholder text contains an English spelling error: `Email addresse` instead of `Email address`. | 1. Open Provider `Team` page.<br>2. Click `Invite staff`.<br>3. Inspect the email placeholder text in the modal. | The email field label reads `Email address`, but the placeholder text says `Email addresse`. | Placeholder copy should use correct English spelling and stay consistent with the field label: `Email address`. | https://s.letweb.net/s/dkmm4o | Task created (HAIRL-1303) | Minor UI copy issue in the Provider invite flow. |
| PR-01-006 | P1 | Team action model / terminology drift | Provider `Team` list exposes `Active` and `Inactive` as the primary context-menu actions, which does not match the FR-009 language of `remove` or `suspend` team members and makes the staff lifecycle harder to understand. | 1. Open Provider `Team`.<br>2. Open the context menu for an invited active staff member.<br>3. Inspect the available actions.<br>4. Compare with FR-009 team-management expectations. | The list-level context menu shows `Active`, `Inactive`, and `View Details`.<br>It does not expose `Suspend` or `Remove` using the FR-009 terminology the sprint scope is written against.<br>Code evidence: `TeamActions.jsx` updates `status` through the generic `PUT team/members/{id}` flow and labels the menu with `Active` / `Inactive`, while FR-009 requirement language calls out removing or suspending team members. | Provider team-management actions should align with FR-009 lifecycle semantics and labels, especially for Owner/Manager operations.<br>If the intended action is suspension, the UI should say `Suspend` / `Reactivate` or another clearly defined lifecycle model instead of a generic `Inactive` toggle that can be confused with account activation state. | https://s.letweb.net/s/g5xxrp | Task created (HAIRL-1304) | FR-009 explicitly states providers must be able to `remove or suspend` team members within permissions.<br>There is a detail-page `Remove or Suspend` action, so the current list menu also creates terminology inconsistency inside the same module. |
| PR-01-007 | P0 | Provider staff RBAC containment | Newly accepted Provider staff accounts across invited clinic roles can open Hairline admin/team-dashboard routes such as `/providers` and `/settings/app-settings`, exposing admin-facing views instead of a provider-scoped staff experience. | 1. Send a Provider staff invitation with a clinic role such as `Manager`, `Clinical Staff`, or `Billing Staff`.<br>2. Complete invitation account setup successfully.<br>3. Log in as the newly created staff account.<br>4. Navigate to routes such as `/providers` and `/settings/app-settings` or use the visible left-nav entries if shown.<br>5. Observe whether the account remains inside provider-scoped RBAC boundaries. | Invited Provider staff can complete account creation and log in, but once signed in they can access Hairline team/admin dashboard surfaces including provider-management and app-settings views.<br>Screenshot evidence already confirms the route exposure on an invited manager account inside `/providers` and `/settings/app-settings`, and follow-up staging review on 2026-06-08 reports the same admin-surface exposure pattern for invited `Clinical Staff` and `Billing Staff` accounts as well.<br>Code evidence points to a system bug, not one bad account setup:<br>`PrivateRoute` only checks `hasRouteAccess()`, and `hasRouteAccess()` currently falls back to allow all routes when `allowedPaths` is empty for `profile_type = provider` because `provider` is included in `STRICT_PERMISSION_PROFILE_TYPES`.<br>Invitation signup creates a `User` model, assigns provider role on that `User`, and issues an `api`-guard Passport token (`user_type = User::class`), not a `ProviderUser` token.<br>Admin/team pages such as `/providers` use APIs like `provider/get-all-providers`, which are exposed under `auth:provider,api`, so the invited staff `api` token can retrieve real provider-management data. | Invited provider staff must remain constrained to the provider RBAC matrix defined by FR-009.<br>Different clinic roles may have different provider-tenant permissions, but none of them should inherit Hairline admin/team-dashboard surfaces or unrestricted admin/provider-management endpoints unless explicitly granted by an admin-side role model.<br>Invitation-created provider staff should authenticate into the provider tenant with provider-scoped route/API access only. | https://s.letweb.net/s/emozxx; https://s.letweb.net/s/ejnnww | Task created (HAIRL-1305) | Confirmed from direct staging behavior plus code review on 2026-06-08.<br>The attached screenshots show the issue on a manager account; follow-up reviewer testing reported the same containment failure for invited `Clinical Staff` and `Billing Staff` accounts after successful account setup and login.<br>This is not just a menu bug: the current codebase combines frontend route fallback over-permissiveness with backend/API guard overlap for invitation-created provider staff.<br>Potential security impact: cross-tenant privilege escalation from provider staff into admin operational surfaces. |
| PR-01-008 | P1 | Team member detail data loss | Provider staff detail page does not show key information that was entered during invited staff account setup, making the member profile feel incomplete and unreliable. | 1. Invite a new staff member and complete account setup with first name, last name, email, and phone number.<br>2. Open Provider `Team` and click `View Details` for that staff member.<br>3. Compare the detail page fields with the information entered during signup. | The detail page shows the member name and email, but phone number is blank and other profile fields such as city/country are also empty.<br>The displayed information is much thinner than the information captured during account creation.<br>Code evidence: `StaffMemberDetail.jsx` expects richer fields such as `phone_number`, `city`, `country`, and `permissions`, but `ProviderTeamController::show()` currently returns the raw `ProviderTeamMember` or fallback `ProviderUser` record without assembling a richer member profile payload. | Staff detail should show the key information captured for that team member, especially the data entered during invitation account setup that is relevant to team management and identity confirmation. | https://s.letweb.net/s/d4rrv7 | Task created (HAIRL-1306) | The current detail screen also contains placeholder-style permissions content, so this surface appears incomplete beyond just the missing phone field. |
| PR-01-009 | P1 | Staff role & permission management | Provider `Role & Permission` modal cannot resolve the current role for newly invited staff and changing role returns server error instead of updating the account. | 1. Invite a new Provider staff member through the public invitation flow and complete account setup.<br>2. Open Provider `Team` -> `View Details` for that invited staff member.<br>3. Click `Role & Permission`.<br>4. Observe the `Team Member Name & Role` card and `Current role` panel.<br>5. Choose a different role and click `Change role`. | The modal opens, but the current role displays as `—` instead of the staff member's existing role.<br>The permission comparison therefore shows `Current role (—)` with no permissions loaded.<br>Submitting `Change role` then returns a generic `Server Error` toast instead of updating the staff role.<br>Code evidence: `TeamMemberDetail.jsx` reads the current role from `member.designation`, `member.user.roles[0].name`, or `member.roles[0].name`, but `ProviderTeamController::show()` returns a raw `ProviderTeamMember`/`User` payload and does not expose the `role_name` accessor it already has for DB-level fallback lookup.<br>For invitation-created staff, `ProviderStaffInvitationController` assigns a provider role onto the `User` model and issues an `api`-guard token, while `User::$guard_name = api`; later `ProviderTeamController::changeRole()` validates against provider-guard roles and calls `syncRoles([$newRole])`, which is not aligned with the invitation-created `User`/guard model and results in server error for these invited staff accounts. | Role & permission management should show the member's actual current role before any change, load the correct current/new permission comparison, and allow valid FR-009 role changes for invited staff without server error.<br>The same staff record type created by the invitation flow should be supported by the role-management flow instead of breaking on a `User` vs `ProviderUser`/guard mismatch. | https://s.letweb.net/s/d7kkxq | Task created (HAIRL-1307) | This is specifically confirmed on an invitation-created staff account (`Liam Foster`) after account setup.<br>The bug appears structurally tied to the mixed identity model already seen elsewhere in PR-01: invitation-created clinic staff are stored as `User` + `provider_team_members`, while other provider-management surfaces still expect `ProviderUser`-style role/designation data. |
|  | P3 | Team member breadcrumb dead link | Provider staff detail breadcrumb includes a `Members` item that looks clickable but routes to a 404, so it does not provide any useful navigation beyond the existing back path to `Team`. | 1. Open Provider `Team`.<br>2. Click `View Details` for a staff member.<br>3. Click the `Members` breadcrumb item in `Dashboard / Team / Members / <member id>`. | The breadcrumb shows a `Members` segment, but clicking it leads to a 404 instead of a valid list page.<br>This makes the breadcrumb misleading because the detail page already has a sensible back path through `Team`, and the `Members` segment adds an unusable navigation target. | Remove the dead `Members` breadcrumb segment, or make it route to a real provider team list page if that is intended to exist.<br>If the breadcrumb cannot navigate anywhere meaningful, it should be simplified so the only actionable back path is `Team`. | TBD | Review pending | Provider dashboard only. Persistent screenshot URL still pending if we want to preserve the visual evidence outside the local screenshot. |
| PR-01-010 | P1 | Detail-page suspend flow not connected | Provider `Remove or Suspend` modal opens from staff detail, but clicking `Suspend Team Member` only shows an informational toast that the flow is not connected to API. | 1. Open Provider `Team` and navigate to a staff member `View Details` page.<br>2. Click `Remove or Suspend`.<br>3. In the modal, click `Suspend Team Member`. | The suspend modal is shown, but submitting it only triggers the toast `Suspend flow not yet connected to API`.<br>No real suspend request is sent, and the member status does not change through the intended FR-009 suspend flow.<br>Code evidence: `pages/providerDashboard/team/TeamMemberDetail.jsx` contains a hardcoded `messageApi.info(\"Suspend flow not yet connected to API\")` instead of wiring the modal submit to the backend suspend endpoint. | If the detail-page suspend modal is exposed in Sprint 1, it should call the actual suspend workflow and produce a real status/access change with any required reassignment logic.<br>If the flow is not implemented yet, the action should be hidden or disabled with clear unavailable-state messaging instead of looking operational. | https://s.letweb.net/s/e6jj0r | Task created (HAIRL-1308) | This is distinct from the generic `Active/Inactive` toggle issue: here the dedicated suspend UI is present but explicitly not implemented. |
| PR-01-011 | P1 | Inactive/reactivate access recovery broken | Setting a staff account to `Inactive` blocks login, but switching the same account back to `Active` does not restore access, leaving no working reactivation path from the Provider team UI. | 1. In Provider `Team`, set an active staff member to `Inactive` from the context menu.<br>2. Attempt to log in with that staff account.<br>3. Return to Provider `Team` and set the same member back to `Active`.<br>4. Attempt to log in again. | After being marked `Inactive`, the staff account can no longer log in.<br>The login screen shows `Your account has not been activated yet. Please check your email for the activation link to set your password.` even though the user had already completed invitation signup and used the account before.<br>Switching the member back to `Active` in the Provider team UI does not restore login access, so the account remains effectively locked without a working recovery path.<br>Code evidence: the generic `PUT team/members/{id}` update path only changes `provider_team_members.status`; it does not reactivate any linked auth-side record or restore access the way the dedicated suspension flow explicitly deactivates linked records and revokes tokens. | If Provider UI supports changing a team member back to an active state, that action must restore the member's ability to authenticate using the same account, or the UI must not present it as a reversible toggle.<br>Status messaging should also distinguish suspended/inactive accounts from never-activated accounts. | https://s.letweb.net/s/e3xxp6; https://s.letweb.net/s/g5xxrp | Task created (HAIRL-1309) | This is separate from the RBAC exposure issue: here the account lifecycle itself becomes inconsistent after the inactive toggle.<br>The current error message is misleading because the account had already completed setup and used login successfully before being set inactive. |
|  | P1 | Task transfer and assigned work reassignment | Scout signal: the reassignment surfaces needed before suspend or remove are still mock or empty in current Provider and Admin detail code. | 1. Open Provider staff detail and inspect reassignment or transfer controls.<br>2. Start the remove or suspend flow that would require handing off work.<br>3. Compare transfer target data and assigned-work content in Provider and Admin staff detail screens. | Provider team-member detail still seeds transfer targets from a hardcoded mock provider list, and the Admin assigned-work tab still uses empty placeholder data with a TODO for the real API hook.<br>This suggests FR-009 task-transfer checks are likely to fail or remain non-testable even after the basic suspend button wiring is fixed. | Reassignment should load real target staff and real assigned-work data before any suspend or remove flow that depends on task transfer. | Code review 2026-06-18 | Scout flagged | Keep this in the first re-test wave after RBAC and suspend blockers, otherwise the team lifecycle pass will look complete while transfer logic is still synthetic. |
|  | P1 | Provider team access | Follow-up PR-01 coverage is still required after the current team-management blockers are fixed, because several role/lifecycle checks cannot be validated against the intended product behavior yet. | After the current blockers are resolved, re-test: 1. role boundary and permission inheritance for invited `Manager`, `Clinical Staff`, and `Billing Staff` inside the correct provider-scoped experience; 2. suspend/remove lifecycle; 3. task transfer/reassignment; 4. session revocation; 5. Owner-protection rules such as preventing removal/demotion of the last Owner. | Invite creation, account setup, accepted login, and Provider `Team` visibility have been exercised, but deeper role/lifecycle coverage is still blocked by current defects.<br>Specifically, non-owner staff currently fall into the admin-style dashboard surface instead of a valid provider role view, the dedicated detail-page suspend flow is not connected to API, and the generic `Active` / `Inactive` toggle does not restore login access cleanly after reactivation. | Once the blocking PR-01 defects are fixed, Sprint 1 still needs a clean regression pass confirming that each clinic role stays inside the correct provider-scoped RBAC boundaries and that team lifecycle actions work end-to-end without breaking access recovery or operational controls. | TBD | Review pending | This row is intentionally a re-test checkpoint, not a confirmed defect row.<br>Blocked follow-up items include role-boundary verification, suspend/remove, task transfer, session revocation, and Owner-protection checks.<br>Educational tour is optional UI polish, not a launch acceptance requirement. |

## PR-02 - Inquiry, Quote & Booking Management

### Review Notes

- Checked areas: Launch-plan scope only.
- Current state: Product behavior not checked in this pass.
- Review limits: Requires provider inquiry receipt, anonymization/de-anonymization, quote build/edit/submit/withdraw, status lifecycle, booking list/detail, and check-in blocking review.

### Remaining Fixes

| Bug ID | Priority | Flow / Story | Issue | Steps to Reproduce | Actual Outcome | Expected Outcome | Evidence Link | Task Status | Notes |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
|  | P0 | Active inquiry questionnaire propagation | Scout signal: provider inquiry review may not match the active Admin questionnaire because intake, persistence, and rendering still come from different sources. | 1. In Admin A-09a, change or publish the active inquiry questionnaire.<br>2. Start a new patient inquiry and answer a distinctive changed question.<br>3. Open the inquiry in provider and admin review and inspect medical answers and alerts. | Patient inquiry still uses a fixed question and medical-history mapping in mobile, while provider and admin inquiry formatting and alerting still read static backend medical-question config.<br>Admin questionnaire-set publication is only partially wired to live endpoints, so provider inquiry review can diverge from what A-09a appears to publish. | The active Admin inquiry questionnaire should control the patient-facing questions and the provider or admin review output consistently. | Code review 2026-06-18 | Scout flagged | Highest-value provider-first manual test inside PR-02 because it can invalidate quote, triage, and booking QA downstream. |
|  | P0 | Provider quote and booking management | Evidence gap: provider-side inquiry, quote, and confirmed-booking workflow has not been verified in staging for this report. | Receive inquiry, confirm pre-payment anonymization, submit quote with appointment slot, withdraw quote with reason, accept quote from patient side, then inspect confirmed booking list/detail and check-in block state. | Not reviewed in this pass. | All eight quote statuses, quote withdrawal, booking context, de-anonymization after deposit, and check-in blocking rules work. | TBD | Review pending | Core cross-tenant blocker until verified. |

## PR-03 - Treatment Execution & Documentation

### Review Notes

- Checked areas: Launch-plan scope only.
- Current state: Product behavior not checked in this pass.
- Review limits: Requires fully-paid confirmed booking fixture and provider treatment documentation review.

### Remaining Fixes

| Bug ID | Priority | Flow / Story | Issue | Steps to Reproduce | Actual Outcome | Expected Outcome | Evidence Link | Task Status | Notes |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
|  | P1 | Fully-paid booking fixture integrity | Scout signal: treatment-execution QA may be fed by inconsistent paid-booking state because deposit success handling is split across old and new payment paths. | 1. Create a booking and pay the Sprint 1 deposit through the current Stripe flow.<br>2. Inspect booking and payment state transitions used to create the treatment fixture.<br>3. Start provider treatment execution only after those states look authoritative. | Backend still has a simulated `pay-deposit` path, the explicit payment-success confirm route is commented out, and the patient app stops after local payment-sheet success handling.<br>This raises a concrete risk that booking and payment flags used by PR-03 fixtures are not reliably synchronized. | The booking state that unlocks treatment execution should be driven by one confirmed payment-success path with consistent booking, payment, and slot-blocking updates. | Code review 2026-06-18 | Scout flagged | Treat this as a fixture-integrity risk; verify before spending time on deep treatment documentation tests. |
|  | P1 | Treatment execution fixture | Evidence gap: treatment execution has not been verified against a fully-paid staging booking fixture in this report. | Use a fully-paid confirmed booking fixture, check in patient when appointment date is due, update day statuses, capture notes, open end-of-treatment modal, and save conclusion notes, prescription, actual graft count, and final head scan link. | Not reviewed in this pass. | Treatment documentation works for Not Started, In Progress, Finished, Need Caution/Attention, Cancelled/Deferred, and required end-of-treatment data. | TBD | Review pending | Real final-balance path is Sprint 2; do not block Sprint 1 on that deferred patient path. |

## PR-06 - Profile & Settings Management

### Review Notes

- Checked areas: Direct Provider dashboard screens for provider profile detail and `Edit Profile -> Staff list`; provider `Settings -> Account Information`; provider `Treatments & Package -> Treatments` list/detail/pricing-config interactions; plus targeted code inspection of shared profile edit components, provider settings account/password flows, navbar identity rendering, and provider treatment/pricing routes/components.
- Positive coverage from the same Account Information pass: changing the provider account email appears to persist successfully, and the user reported that logging back in with the updated email still works. This suggests the basic account-email update path is functional even though other account-settings mappings remain broken.
- Additional pass coverage from the same Account Information pass: timezone changes save successfully; `Cancel` resets edited values back to the last saved state; and saving individual fields or multiple account-information fields together appears to persist normally. The remaining account-settings gaps are therefore concentrated in mapping/validation/password behavior rather than the entire save/reset flow.
- Positive coverage from direct `Notification Preferences` review: individual notification toggles can be switched on/off, `Enable All` works, `Disable All` shows a warning dialog, saving and reloading persist the saved settings, and saving after all notification types are disabled also shows a warning confirmation. This suggests the basic persistence and all-disabled safeguard paths are implemented.
- Positive coverage from direct `Billing` review: Dataset A bank details can be created successfully, saved data persists after reload, existing bank details can be edited across single or multiple fields, `Cancel` resets unsaved edits back to the last saved values, and the current form enforces the visible 200-character cap on `Account Holder Name` and `Bank Name`.
- Current state: Direct provider-dashboard review now confirms four separate PR-06 mismatch clusters. First, a staff-surface mismatch remains: Provider `Team` can show four active invited staff, but Provider profile detail and `Edit Profile -> Staff list` still render only the legacy two-person `provider_users` roster. The shared `Add New Staff` button in the profile edit `Staff list` can also blank the page instead of opening a usable modal. Second, the Provider treatment-pricing flow is materially misaligned with FR-024: treatment cards hide primary detail access inside the three-dot menu, `/treatments/:id` reuses the wrong admin/team-style detail surface, provider pricing configuration is implemented as a four-step wizard with admin-style read-only treatment/edit/media sections instead of the PRD's provider pricing screen, the Step 1 `Active treatment` switch misrepresents provider-vs-admin status semantics, successful pricing save does not flow back into list pricing display, the currency selector is hardcoded, and the exposed `Deactivate` action is unwired. Third, the `Account Information` tab is materially misaligned with FR-032: the Owner read-only `Account name` does not track the clinic profile name, phone-country-code parsing corrupts the saved number when the form is reloaded, password save attempts surface a generic `Unauthenticated.` toast for both invalid and valid password attempts, first/last name fields accept special characters and digits despite the PRD restriction, and the saved name does not stay consistent across Account Settings, Provider Profile staff cards, and the top navbar identity. Fourth, the `Billing` tab now shows working persistence but still diverges from FR-032 in validation and unsaved-change UX: tab-level unsaved indicators / leave warnings are missing, account-holder naming rules are too permissive, and validation feedback timing is inconsistent across fields.
- Code evidence checked: Admin route `/edit-provider/:id` renders `EditProfile isTeam`, which calls `ProfileSettingsTab`; Provider edit profile also uses the same `ProfileSettingsTab`. Child tabs include shared `Profile`, `Language`, `Staff`, `AwardsTab`, `Reviews`, and `Documents` components, so fixes to Admin edit controls should regression-test Provider PR-06 surfaces. Provider `/treatments/:id` is also routed to the shared `teamDashboard/treatments/TreatmentDetails` component, and provider pricing uses a separate `ProviderTreatmentPricingConfig` wizard plus hardcoded step subcomponents. For account settings, `AccountInformation.jsx` parses saved phone numbers with a greedy country-code regex, `ProviderSettingsController::getAccountInfo()` derives Owner `account_name` from `$provider->name` even though the provider model/migration use `provider_name`, and the shared navbar renders provider identity directly from the Redux auth user (`first_name` / `last_name`) rather than refreshing after account-info updates. For notification preferences, the component renders each notification type as a `Collapse` panel with `expandIcon={() => null}` and places the `Switch` inside the same collapsible header, while the parent settings tab only tracks unsaved-change state for `Notification Preferences` and not for `Account Information`. For billing, `BillingSettings.jsx` contains a local `hasUnsavedChanges()` helper that is not surfaced to the parent tab or `beforeunload` flow, only `Routing/SWIFT` has an inline `pattern` rule while `IBAN` is also revalidated again inside the save handler, and neither frontend nor backend enforces the PRD's `Account Holder Name = alphabetic and spaces only` rule.
- Review limits: Requires direct provider-dashboard review for the rest of PR-06 scope that has not yet been covered, including package catalog, full staff CRUD after the current crash is fixed, language ordering/search behavior, quote-creation treatment/package selection, and confirmation whether duplicate awards in API are expected test data or an update bug.

### Remaining Fixes

| Bug ID | Priority | Flow / Story | Issue | Steps to Reproduce | Actual Outcome | Expected Outcome | Evidence Link | Task Status | Notes |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| PR-06-001 | P1 | Provider profile staff surfaces | Provider `Provider Profile` detail and `Edit Profile -> Staff list` do not reflect the current invited team roster that already appears in Provider `Team`. | 1. Create and accept invited staff accounts in Provider `Team` until four staff are active.<br>2. Open Provider `Team` and confirm the active list.<br>3. Open Provider `Provider Profile` detail.<br>4. Open `Edit Profile -> Staff list` for the same provider.<br>5. Compare the rendered staff cards across the surfaces. | Provider `Team` shows four active staff rows, but Provider `Provider Profile` detail and Provider `Edit Profile -> Staff list` still show only `Dr. Michael Johnson` and `Mehmet Arslan`.<br>These profile-related surfaces do not match the current roster that the same tenant can see in `Team`.<br>Code evidence: Provider profile/detail/edit staff UI still consumes `provider.provider_users` from `get-single-provider`, while Provider `Team` is driven by the newer `team/members` flow. | Provider profile detail and edit surfaces should render the clinic's current accepted staff roster, consistent with Provider `Team` and with whichever backend entity is now canonical for FR-009 team management. | https://s.letweb.net/s/dn55p0; https://s.letweb.net/s/epmmrj; https://s.letweb.net/s/dqvvn9 | Task created (HAIRL-1310) | This is the Provider-tenant manifestation of the larger staff-roster split between `provider_team_members` and `provider_users`.<br>Admin surfaces show the same stale two-member roster, so the final fix needs cross-tenant regression coverage. |
| PR-06-002 | P1 | Provider profile staff editor crash | Clicking `Add New Staff` in profile-edit `Staff list` can blank the page instead of opening a working modal. | 1. Open Provider `Edit Profile` or Admin `Edit Provider` for `test_provider3@clinic.com`.<br>2. Go to `Staff list`.<br>3. Click `Add New Staff`.<br>4. Observe the page state and console. | The page turns blank/white instead of keeping the form mounted and opening a usable staff modal.<br>Console shows an uncaught `TypeError: h.map is not a function`.<br>Code evidence: `StaffModal.jsx` calls `const roleOptions = roleData?.map(...)`, but `ProviderTeamController::getRoles()` returns an object with `{ status, data }`, not a bare array, so opening the modal can crash when the roles response is consumed with the wrong shape. | `Add New Staff` should open a stable staff modal in both Provider and Admin profile-edit flows, and role options should be read from the actual API response shape without crashing the React tree. | https://s.letweb.net/s/dw779k | Task created (HAIRL-1311) | Shared-component issue: Provider edit profile and Admin `/edit-provider/:id` both use `ProfileSettingsTab` and the same `Staff` / `StaffModal` implementation, so this crash affects both tenants. |
| PR-06-003 | P1 | Account Information -> Owner Account Name Sync | Owner `Account name` is read-only as if it were synced to clinic name, but it actually shows a personal-name fallback and does not update when the clinic profile name changes. | 1. Open Provider `Profile Setting -> Basic info` for `test_provider3@clinic.com` and note the clinic name such as `Joachim VN Provider Test 3` / `VN Provider Test 3`.<br>2. Open Provider `Settings -> Account Information` for the same Owner account.<br>3. Compare the read-only `Account name` field with the clinic name shown in profile surfaces.<br>4. Change the clinic name in profile edit, save, and return to `Account Information`. | The read-only `Account name` field shows `Dr. Michael` instead of the clinic profile name and remains unchanged after the clinic name is updated from `Joachim VN Provider Test 3` to `VN Provider Test 3`.<br>This contradicts the helper text that says the field is synced with clinic name.<br>Code evidence: `ProviderSettingsController::getAccountInfo()` derives Owner `account_name` from `$provider->name`, but the provider model/migration use `provider_name`; when `name` is absent, the code falls back to `$providerUser->first_name`, which matches the observed personal-name display. | For Owner accounts, the read-only `Account name` field should display the current clinic name from Provider Profile and update when the clinic name changes, as defined by FR-032. | https://s.letweb.net/s/gryymq; https://s.letweb.net/s/dw7765; https://s.letweb.net/s/g2xxvn | Task created (BE: HAIRL-1339) | Smallest code surfaces: `main/hairline-backend/app/Http/Controllers/Settings/ProviderSettingsController.php`, `main/hairline-backend/app/Models/Provider.php`, and `main/hairline-backend/database/migrations/2024_01_31_154624_create_providers_table.php`.<br>Provider profile banner separately renders `data.provider_name`, which reinforces that the settings sync path is reading the wrong provider field. |
| PR-06-004 | P1 | Account Information -> Phone Country Code Re-Parse | Saving a provider phone number with a valid country code can re-open the form in a corrupted split state where part of the local number is reinterpreted as the country code. | 1. Open Provider `Settings -> Account Information`.<br>2. Change `Phone number` to country code `+44` and local number `7700900123`.<br>3. Save the form and return to the same tab or reload the page.<br>4. Inspect the split country-code and number inputs. | After save/reload, the phone field is re-parsed as `+4477` and `00900123` instead of staying `+44` and `7700900123`.<br>This is a direct Provider-dashboard bug, separate from the earlier Admin shared-phone-input report.<br>Code evidence: `AccountInformation.jsx` rehydrates the saved phone number with `/^(\\+\\d{1,4})(.*)$/`, so a stored value like `+447700900123` greedily captures `+4477` as the country code and shifts the remaining digits into the local-number input. | The form should preserve a valid saved number as the same country code and local number after refresh, or store/read the phone components in a way that cannot corrupt the split inputs. | https://s.letweb.net/s/evqqm5; https://s.letweb.net/s/eykkvx; https://s.letweb.net/s/e3xxqm | Task created (FE: HAIRL-1340) | Smallest code surface: `main/hairline-frontend/src/components/providerComponents/providerSettings/AccountInformation.jsx` phone rehydration logic.<br>This row intentionally records a new Provider-side bug instead of reusing the earlier Admin A-02 shared-phone-input row. |
| PR-06-005 | P1 | Account Information -> Password Validation Error Messaging | When the new password does not meet the visible password requirements, saving the form still ends in a generic `Unauthenticated.` toast instead of a clear password-validation error. | 1. Open Provider `Settings -> Account Information`.<br>2. Expand `Change password`.<br>3. Enter current password `password`, new password `password123`, and confirm password `password123`.<br>4. Click `Save changes`.<br>5. Observe the error feedback. | The UI shows a small `Unauthenticated.` toast near the top of the form.<br>It does not explain which password rule failed or whether the current password, auth session, or backend validation caused the failure.<br>Code evidence: the frontend saves account info first and only then attempts the password update, while the backend controller itself would normally return structured `422 Validation failed` or `400 Current password is incorrect` responses if the request reached that logic. | Password errors should be explicit and actionable, such as showing the failed password rule or a clear current-password error, not a generic auth toast that gives no indication how to fix the input. | https://s.letweb.net/s/dz662p | Task created (FE: HAIRL-1341; BE: HAIRL-1342) | Smallest code surfaces: `main/hairline-frontend/src/components/providerComponents/providerSettings/AccountInformation.jsx` combined save flow and `main/hairline-backend/app/Http/Controllers/Settings/ProviderSettingsController.php` password validation branches.<br>The observed `Unauthenticated.` message suggests the request is failing outside the intended validation UX path. |
| PR-06-006 | P1 | Account Information -> Valid Password Change Still Fails | Even when the new password satisfies all visible requirements, the password update still fails with `Unauthenticated.` and the password does not change. | 1. Open Provider `Settings -> Account Information`.<br>2. Expand `Change password`.<br>3. Enter current password `password`, new password `Password%123`, and confirm password `Password%123`.<br>4. Confirm the UI shows `All requirements met`.<br>5. Click `Save changes`. | The UI still shows `Unauthenticated.` and the password is not updated, even though the visible client-side requirements are satisfied.<br>Code evidence: the provider settings password endpoint exists and the controller has explicit `422`, `400`, and `200` branches; since the observed runtime outcome is still `Unauthenticated.`, the failure likely happens before the controller can complete its intended logic or in auth/session handling around this call path. | A valid provider password change should complete successfully, update the stored password, and give a success message plus security notification as defined in FR-032. | https://s.letweb.net/s/d1qqvl | Task created (BE: HAIRL-1343) | Smallest code surfaces: `main/hairline-backend/routes/api.php` provider settings route group, `main/hairline-backend/app/Http/Controllers/Settings/ProviderSettingsController.php`, and `main/hairline-frontend/src/components/providerComponents/providerSettings/AccountInformation.jsx` combined save sequence.<br>Likely source should be investigated in backend auth/session handling, not only UI copy. |
| PR-06-007 | P2 | Account Information -> Name Validation Missing | First and last name fields accept special characters and digits, and the saved values propagate into provider staff surfaces even though FR-032 restricts these fields to letters and spaces only. | 1. Open Provider `Settings -> Account Information`.<br>2. Enter `Dr.% Michael` as `First name` and `Johnson123` as `Last name`.<br>3. Save the form.<br>4. Open Provider `Provider Profile` and inspect the staff card for the same Owner account. | The form saves successfully with `Dr.% Michael` and `Johnson123`.<br>The saved values then appear in the Provider Profile staff card, showing that the invalid name values were persisted rather than rejected.<br>Code evidence: the backend account update validation only requires `string/max:255`, and the frontend inputs do not enforce the FR-032 `letters and spaces only` rule. | First and last name fields should reject digits and special characters according to FR-032, or the product/docs must be updated if a broader naming policy is intentionally allowed. | https://s.letweb.net/s/d4rrq4; https://s.letweb.net/s/g5xx3l | Task created (FE: HAIRL-1344; BE: HAIRL-1345) | PRD source of truth: `fr032-provider-dashboard-settings/prd.md` says `First Name` and `Last Name` allow letters and spaces only, max 50 chars.<br>Smallest code surfaces: `main/hairline-frontend/src/components/providerComponents/providerSettings/AccountInformation.jsx` and `main/hairline-backend/app/Http/Controllers/Settings/ProviderSettingsController.php`. |
| PR-06-008 | P1 | Account Information / Navbar / Profile -> Display Name Mapping Drift | Saving account-level name changes updates some provider surfaces but leaves the top navbar identity stale, revealing divergent name sources across clinic profile, account settings, and auth-session display. | 1. Save changed account first/last names from Provider `Settings -> Account Information`.<br>2. Open Provider `Provider Profile` and inspect the Owner staff card.<br>3. Open the top-right navbar identity dropdown on both `Provider Profile` and `Settings`.<br>4. Compare the displayed names against `Account Information` and clinic profile name surfaces. | After saving `Dr.% Michael` / `Johnson123`, the Provider Profile staff card reflects the new owner name, but the top navbar dropdown still shows the old `Dr. Michael Johnson` value.<br>At the same time, the read-only `Account name` field remains a separate value/source from the clinic profile banner (`VN Provider Test 3`).<br>Code evidence shows at least three different naming sources: Provider Profile banner renders `provider_name`; Account Settings derives Owner `account_name` from the provider settings endpoint; the navbar renders `auth.user.first_name` and `auth.user.last_name` from Redux/local storage and is not refreshed after account-info save. | Provider surfaces should use a clear, documented name model and refresh consistently after account-info changes, rather than leaving navbar identity, account settings, and clinic profile on separate stale mappings. | https://s.letweb.net/s/g2xxvn; https://s.letweb.net/s/g5xx3l; https://s.letweb.net/s/e6jjv3 | Task created (FE: HAIRL-1346; BE: HAIRL-1347) | This appears broader than one stale text label: current behavior suggests clinic name (`provider_name`), account-name sync, and navbar auth identity are separate mappings with inconsistent refresh rules.<br>Smallest code surfaces: `main/hairline-frontend/src/components/shared/navBar/NavBar.jsx`, `main/hairline-frontend/src/features/auth/authSlice.jsx`, `main/hairline-frontend/src/components/providerComponents/providerProfile/ProfileBanner.jsx`, and `main/hairline-backend/app/Http/Controllers/Settings/ProviderSettingsController.php`. |
| PR-06-009 | P1 | Notification Preferences -> Expand Interaction And Disclosure Cue | Notification-type rows behave like collapsible sections but the expand interaction is not clearly separated from the toggle interaction, and the UI gives no visual cue that each row can expand to show included notification examples. | 1. Open Provider `Settings -> Notification Preferences`.<br>2. Review any notification-type row such as `Treatment Start Notifications` or `Review Notifications`.<br>3. Toggle the switch and also click around the row header to reveal the detailed examples block.<br>4. Compare the live interaction with the PRD expectation for collapsible sections. | The product currently reveals an `Examples of notifications included` block under a row, but there is no chevron or other disclosure indicator showing the row is expandable.<br>The expand/collapse behavior is not clearly separated from the on-row toggle interaction, so changing a preference can also feel tied to opening the row details instead of being two distinct controls.<br>PRD notes call for collapsible sections with examples, but the current interaction model is ambiguous and under-signaled.<br>Code evidence: `NotificationPreferences.jsx` renders notification types as `Collapse` panels, explicitly hides the expand icon with `expandIcon={() => null}`, and places the `Switch` inside the same collapsible header area. | Each notification-type row should make it obvious that detailed examples can be expanded/collapsed, and the detail-disclosure interaction should be visually and behaviorally distinct from the on/off toggle control. | https://s.letweb.net/s/gl992l; https://s.letweb.net/s/emoo4y | Task created (FE: HAIRL-1348) | PRD source of truth: FR-032 Screen 3 notes say notification types should be displayed in collapsible sections with examples.<br>Smallest code surface: `main/hairline-frontend/src/components/providerComponents/providerSettings/NotificationPreferences.jsx`. |
| PR-06-010 | P1 | Account Information -> Missing Unsaved Changes Indicator Consistency | `Notification Preferences` shows the PRD-style unsaved-changes indicator on the tab label plus a pending-changes message near the save area, but `Account Information` does not expose the same tab-level unsaved state behavior. | 1. Open Provider `Settings -> Notification Preferences`.<br>2. Change any toggle without saving and observe the tab label and footer near `Save Preferences`.<br>3. Switch to `Account Information`.<br>4. Edit fields such as `First name` or `Time zone` without saving.<br>5. Compare whether the same unsaved-changes indicator pattern appears. | `Notification Preferences` correctly shows a small dot on the tab label and a `You have unsaved changes` message near the save button after edits.<br>`Account Information` does not expose the same tab-level unsaved-change indicator behavior, so the settings tabs are inconsistent even though both are part of the same PRD-defined settings pattern.<br>Code evidence: `ProviderSettingTab.jsx` only tracks `notificationTabHasUnsaved` for the notification tab label, while `AccountInformation.jsx` has no corresponding unsaved-change tracking state or parent callback. | Per FR-032, each settings tab should support its own save flow and show an unsaved-changes indicator on the tab label when edits are made, with consistent behavior across `Account Information`, `Notification Preferences`, and other settings tabs. | https://s.letweb.net/s/dkmm33; https://s.letweb.net/s/emoo4y | Task created (FE: HAIRL-1349) | PRD source of truth: FR-032 business rules say each tab shows an unsaved-changes indicator when edited and warns on navigation away.<br>Smallest code surfaces: `main/hairline-frontend/src/components/providerComponents/providerSettings/ProviderSettingTab.jsx` and `main/hairline-frontend/src/components/providerComponents/providerSettings/AccountInformation.jsx`. |
| PR-06-011 | P1 | Billing -> Missing Unsaved Changes Indicator And Leave Warning Consistency | The `Billing` tab persists data correctly, but edits do not trigger the same tab-level unsaved indicator or reload/close warning behavior already present in `Notification Preferences`. | 1. Open Provider `Settings -> Billing`.<br>2. Click `Edit Bank Account Details` and change one or more fields without saving.<br>3. Observe the tab label and page footer for unsaved-change messaging.<br>4. Reload the browser tab or attempt to close the page before saving.<br>5. Compare the behavior with `Notification Preferences`. | Billing edits do not add the small unsaved-change dot on the tab label and do not enforce the same reload/close warning pattern seen in `Notification Preferences`.<br>This leaves Billing inconsistent with the settings-tab UX pattern already present elsewhere in the same screen.<br>Code evidence: `BillingSettings.jsx` defines a local `hasUnsavedChanges()` helper, but `ProviderSettingTab.jsx` only tracks unsaved state for `Notification Preferences`, and Billing does not register any `beforeunload` or parent callback behavior. | Per FR-032, each settings tab should support its own save flow and show an unsaved-changes indicator on the tab label when edited, with warning behavior when the user attempts to leave with unsaved changes. | https://s.letweb.net/s/govv59 | Task created (FE: HAIRL-1350) | PRD source of truth: FR-032 business rules/notes say each settings tab shows an unsaved-changes indicator and warns on navigation away when edits are pending.<br>Smallest code surfaces: `main/hairline-frontend/src/components/providerComponents/providerSettings/BillingSettings.jsx` and `main/hairline-frontend/src/components/providerComponents/providerSettings/ProviderSettingTab.jsx`. |
| PR-06-012 | P1 | Billing -> Account Holder Name Validation Missing | `Account Holder Name` accepts digits and special characters and can still be saved, even though FR-032 restricts this field to alphabetic characters and spaces only. | 1. Open Provider `Settings -> Billing` and enter edit mode.<br>2. Set `Account Holder Name` to a value such as `Clinic 123 @`.<br>3. Keep the remaining fields valid and save the form.<br>4. Reload the tab and review the stored value. | The form allows `Clinic 123 @` to persist successfully instead of rejecting it.<br>Current runtime behavior only enforces required/max-length checks on this field, not the PRD's alphabetic-and-spaces rule.<br>Code evidence: `BillingSettings.jsx` only applies required/max rules for `account_holder_name`, and `BankingDetailsController.php` backend validation also accepts any `string max` value. | `Account Holder Name` should reject digits and special characters, or the PRD must be updated if broader naming is intentionally allowed for payout accounts. | https://s.letweb.net/s/epmmw7; https://s.letweb.net/s/dqvvx4; https://s.letweb.net/s/gryyrq | Task created (FE: HAIRL-1351; BE: HAIRL-1352) | PRD source of truth: FR-032 Screen 4 defines `Account Holder Name` as max 200 and `alphabetic and spaces only`.<br>Smallest code surfaces: `main/hairline-frontend/src/components/providerComponents/providerSettings/BillingSettings.jsx` and `main/hairline-backend/app/Http/Controllers/Settings/BankingDetailsController.php`. |
| PR-06-013 | P2 | Billing -> Validation Feedback Timing And Visual State Inconsistent | Billing field validation is not presented consistently: `Routing/SWIFT` shows inline validation before save, `IBAN` only surfaces blocking feedback at save time in this test pass, and field-length/input-limit feedback is not visually consistent across the form. | 1. Open Provider `Settings -> Billing` and enter edit mode.<br>2. Enter an invalid `Routing/SWIFT Code` such as `ABC12` and observe the field feedback before save.<br>3. Enter an invalid `IBAN` such as `TR12 34` and attempt to save.<br>4. Try boundary-length input on `Account Number`, `Routing/SWIFT Code`, and `IBAN` and compare the visual/error feedback each field provides. | In this review, `Routing/SWIFT Code` immediately shows an inline error, while `IBAN` only surfaced the blocking validation when `Save` was attempted.<br>The form also stops extra input at certain field limits without giving the same red/error-state treatment across all affected fields, so the validation feedback model feels inconsistent from one billing field to another.<br>Code evidence: `BillingSettings.jsx` mixes field-level rules (`pattern`, `maxLength`, custom validator) with extra save-time checks such as `validateIBAN()` inside `handleSaveBillingInfo()`, producing non-uniform validation timing and feedback. | Billing validation should give users consistent, predictable feedback across fields about when an input is invalid and why, especially for format-sensitive fields like SWIFT and IBAN. | https://s.letweb.net/s/epmmw7; https://s.letweb.net/s/dqvvx4; https://s.letweb.net/s/dw7705; https://s.letweb.net/s/gxjjrq | Task created (FE: HAIRL-1353) | This row is about UX/validation consistency rather than one specific backend format rule.<br>Smallest code surface: `main/hairline-frontend/src/components/providerComponents/providerSettings/BillingSettings.jsx`. |
| PR-06-014 | P1 | Provider Treatments List -> Open Treatment Detail | Provider treatment cards do not provide a primary clickable path into treatment detail; the user must open the hidden three-dot context menu and choose `View`. | 1. Open Provider `Products & Services -> Treatments & Packages -> Treatments`.<br>2. Click directly on a treatment card body, thumbnail, title, or visible pricing area.<br>3. Observe whether detail opens.<br>4. Open the three-dot menu and choose `View`.<br>5. Compare the interaction paths. | Clicking the visible treatment card does not open the treatment detail page.<br>The only working route into detail is currently hidden inside the three-dot menu `View` action.<br>Code evidence: the provider list card component renders the card as a plain `<div>` and only wraps `View` inside a dropdown menu link; there is no primary link/button around the card body. | Provider treatment browsing should expose an obvious primary detail-access path from the card itself, consistent with the PRD's card-based browsing model for admin-created treatments and without forcing users to discover a hidden context-menu action for basic navigation. | https://s.letweb.net/s/ejnnvw | Task created (HAIRL-1325) | Smallest code surface: `main/hairline-frontend/src/components/providerDetailsComponents/Treatment.jsx` card wrapper and dropdown menu.<br>This is primarily a provider UX/interaction bug, not a backend data issue. |
| PR-06-015 | P1 | Provider Treatment Detail -> Wrong Surface / Structure | Provider `/treatments/:id` is rendering the wrong detail surface: it reuses an admin/team-style treatment detail page with `in progress`, `End Treatment`, and package tabs, which does not match FR-024 provider treatment-pricing behavior or the supplied design. | 1. Open Provider `Products & Services -> Treatments`.<br>2. Use the card menu `View` action on an existing treatment such as `Sapphire FUE Hair Transplant`.<br>3. Inspect the resulting detail page structure and available actions.<br>4. Compare the page against FR-024 Provider scope for treatment pricing configuration. | The page shows a status badge `in progress`, an `End Treatment` CTA, and a package-tab layout, which is structurally unrelated to provider treatment-offering management in FR-024.<br>PRD scope for providers is `Products & Services -> Treatments > Treatment Pricing`, where providers view admin-created treatment information and configure offering/pricing; it is not a treatment-execution or admin/team detail page.<br>Code evidence confirms the route mismatch: provider `/treatments/:id` is wired to the shared `teamDashboard/treatments/TreatmentDetails` component rather than a provider-specific treatment detail surface. | Provider treatment detail should follow the provider treatment-pricing contract from FR-024: provider reads admin-owned treatment info, then configures pricing/offering in the correct provider flow, without unrelated `End Treatment`/execution semantics or admin/team-style package-detail reuse. | https://s.letweb.net/s/dkmm0o | Task created (HAIRL-1326) | PRD source of truth: `fr024-treatment-package-management/prd.md` Screen 3 `Provider Treatment Pricing Configuration`.<br>Smallest code surfaces: `main/hairline-frontend/src/App.jsx` provider route `/treatments/:id` and `main/hairline-frontend/src/pages/teamDashboard/treatments/TreatmentDetails.jsx`. |
| PR-06-016 | P1 | Provider Treatment Pricing -> Step Structure And Status Semantics | Provider treatment pricing is implemented as an admin-style four-step wizard and uses a misleading Step 1 `Active treatment` switch, even though FR-024 defines a provider pricing screen where the actionable provider status is `Offered / Not Offered`, separate from admin's global treatment active/inactive state. | 1. Open Provider `Treatments -> Configure Pricing` for an existing treatment.<br>2. Review Step 1 fields and the `Active treatment` switch.<br>3. Continue to Step 3 `Price` and inspect the `Status` toggle `Offered / Not Offered`.<br>4. Compare the semantics and step structure against FR-024 Screen 3. | The provider flow is presented as a four-step wizard (`Treatment`, `Upload video / image / document`, `Price`, `Summary`) instead of the PRD's provider pricing configuration surface.<br>Step 1 includes a disabled `Active treatment` switch that suggests provider control over treatment activation, but provider control in FR-024 is the offering toggle `Offered / Not Offered` tied to pricing; global treatment activation/deactivation belongs to admin only.<br>This makes the provider status model ambiguous and pushes the real provider toggle to Step 3 after an unrelated admin-style read-only/media flow. | Provider treatment pricing should reflect FR-024 semantics clearly: admin owns global treatment active/inactive status, while provider controls only whether the clinic offers that treatment and at what price.<br>The provider UI should not expose a misleading `Active treatment` control or reuse an admin-style multi-step treatment-edit wizard for a provider pricing task. | https://s.letweb.net/s/gl9949; https://s.letweb.net/s/emooqj; https://s.letweb.net/s/ejnnvv | Task created (HAIRL-1327) | PRD source of truth: `REQ-024-003` admin global active/inactive; Screen 3 provider `Status Toggle = Offered / Not Offered`.<br>Smallest code surfaces: `main/hairline-frontend/src/pages/providerDashboard/treatments/pricingConfig/ProviderTreatmentPricingConfig.jsx`, `.../steps/TreatmentStep.jsx`, and `.../steps/PriceStep.jsx`. |
| PR-06-017 | P1 | Provider Treatment Pricing -> Save Success But List Price Stays Stale | Provider can update treatment pricing and receive a success toast, but the treatment list still shows the old price because the list card is not reading the provider pricing fields that were just saved. | 1. Open Provider `Treatments -> Configure Pricing` for an existing treatment.<br>2. Change pricing values such as `Base Price` and `Price per graft`.<br>3. Continue to `Summary` and click `Update`.<br>4. Observe the success toast.<br>5. Return to the treatments list and compare the card price display. | The flow shows `Treatment pricing updated successfully`, but the treatment card list still shows the previous price value instead of the newly saved provider pricing.<br>Code evidence shows the provider list cards render top-level `base_price` / `currency`, while the provider treatment list API attaches provider-specific pricing inside `treatment.pricing.{base_price,currency,...}` and flags `is_pricing_configured` separately.<br>As a result, the update can persist server-side but the list UI continues reading the wrong field path for provider pricing display/filtering. | After a successful provider pricing save, the treatment list should display the current provider pricing values and any related filters/badges should reflect the updated configuration immediately or after refresh. | https://s.letweb.net/s/ejnnvv; https://s.letweb.net/s/dkmm03; https://s.letweb.net/s/ejnnvw | Task created (HAIRL-1328) | Smallest code surfaces: `main/hairline-frontend/src/components/providerDetailsComponents/Treatment.jsx`, `main/hairline-frontend/src/pages/providerDashboard/treatments/Treatments.jsx`, and `main/hairline-backend/app/Http/Controllers/Packages/TreatmentController.php` index mapping of `pricing` vs top-level treatment fields. |
| PR-06-018 | P1 | Provider Treatment Pricing -> Currency Source Mismatch | The currency selector in provider treatment pricing appears hardcoded and is not aligned with the PRD requirement that provider default currency be pre-selected. | 1. Open Provider `Treatments -> Configure Pricing`.<br>2. Go to Step 3 `Price`.<br>3. Open the `Currency` dropdown.<br>4. Compare the available options and default value behavior with PRD expectations. | The provider pricing UI exposes a fixed hardcoded list `USD / EUR / GBP / VND` in the frontend component.<br>When no prior pricing exists, the screen initializes `currency` as empty rather than pre-selecting the provider's default currency as required by FR-024.<br>Backend validation only enforces a 3-character currency code and controller fallback can derive provider default currency server-side, but the UI itself is not sourcing the dropdown from a centralized currency/config source or the provider account default. | Provider treatment pricing should pre-select the provider's default currency and ideally source the selectable currency list from the system's authoritative configuration, not a local hardcoded frontend array. | https://s.letweb.net/s/gl994l | Task created (HAIRL-1329) | PRD source of truth: Screen 3 says `Currency` is provider-default pre-selected and can be changed per treatment.<br>Smallest code surfaces: `main/hairline-frontend/src/pages/providerDashboard/treatments/pricingConfig/steps/PriceStep.jsx`, `.../ProviderTreatmentPricingConfig.jsx`, and backend request/controller currency fallback. |
| PR-06-019 | P1 | Provider Treatments List -> Deactivate Action Unwired | The treatment-card context menu exposes `Deactivate`, but clicking it has no product effect and the action does not match the implemented PRD control model. | 1. Open Provider `Products & Services -> Treatments`.<br>2. Open the three-dot menu on a treatment card.<br>3. Click `Deactivate`.<br>4. Observe UI feedback, network activity, and treatment state changes. | Clicking `Deactivate` produces no visible response, no confirmation flow, and no treatment-state change.<br>Code evidence shows the action only runs `console.log(\"Deactivate treatment:\", id)` and does not call any mutation.<br>PRD also defines provider control as `Offered / Not Offered` within treatment pricing configuration; it does not define a separate unwired list-menu deactivate action for provider treatments. | The provider surface should either implement a working offering-status change that matches FR-024 semantics or remove this exposed menu action until it is correctly wired.<br>Provider treatment availability should be controlled through the real offered/not-offered pricing state, not a dead menu item. | https://s.letweb.net/s/emooqy | Task created (HAIRL-1330) | Smallest code surface: `main/hairline-frontend/src/components/providerDetailsComponents/Treatment.jsx` dropdown `deactivate` handler.<br>This is both a dead action bug and a PRD-alignment issue. |
|  | P1 | Provider clinic profile and package catalog | Evidence gap: the remaining PR-06 profile/settings/package-catalog areas outside the already-reviewed `Account Information`, `Notification Preferences`, `Billing`, provider staff-surface, and treatment-pricing paths have not yet been fully verified in staging. | Continue PR-06 review by checking the remaining provider profile tabs and settings/package-catalog paths: `Languages`, `Awards`, `Reviews`, `Documents`, and any still-exposed provider package-catalog create/edit entries. Also re-check role/permission-related profile settings only where they directly affect provider-facing behavior. | `Account Information`, `Notification Preferences`, `Billing`, and provider treatment-pricing were directly reviewed in this pass, but the remaining profile/settings/package-catalog areas listed above were not yet fully checked end-to-end. | Provider can maintain the rest of the international-facing clinic profile and provider package catalog according to Sprint 1 scope, with each remaining PR-06 surface directly validated on staging. | TBD | Review pending | Languages tab is critical for international scope.<br>This row now intentionally tracks only the PR-06 areas still pending after direct Account Information, Notification Preferences, Billing, and treatment-pricing review. |

## A-01 - Patient Management & Booking Oversight

### Review Notes

- Checked areas: Launch-plan scope only.
- Current state: Product behavior not checked in this pass.
- Review limits: Requires admin patient, inquiry, quote, booking, manual intervention, support action, and audit-trail review.

### Remaining Fixes

| Bug ID | Priority | Flow / Story | Issue | Steps to Reproduce | Actual Outcome | Expected Outcome | Evidence Link | Task Status | Notes |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
|  | P1 | Admin billing detail and payment actions | Scout signal: patient billing oversight still relies on large frontend mock data blocks and simulated actions. | 1. Open Admin patient billing detail and payment tabs.<br>2. Trigger any available invoice or payment actions and refresh.<br>3. Compare what changed in UI against real payment data sources. | Patient billing detail still renders mock invoice and payment history directly in the frontend component, several actions only simulate success or toast states, and refund is explicitly marked as coming soon.<br>This is a strong sign that A-01 billing oversight may look operational before manual test while not persisting or coordinating with real payment state. | Admin billing oversight should read authoritative patient payment data and perform real supported actions with persisted results. | Code review 2026-06-18 | Scout flagged | Coordinate this with S-02 because billing oversight depends on real payment records. |
|  | P2 | Patient flags and alerts visibility | Scout signal: Admin patient detail payload still returns empty `flags` and `alerts`, suggesting clinical or support risk surfacing may be missing even when inquiry answers should produce one. | 1. Open Admin patient detail for a case with notable inquiry answers.<br>2. Inspect risk or alert surfaces in the patient overview.<br>3. Compare with provider or inquiry alert expectations. | Current backend patient detail response still hardcodes empty `flags` and `alerts` arrays, so Admin oversight may be missing a real signal path rather than only lacking staging evidence. | Admin patient oversight should surface the relevant flags and alerts derived from inquiry and care state where PRD-supported. | Code review 2026-06-18 | Scout flagged | Interacts with the questionnaire and inquiry source-of-truth risk. |
|  | P1 | Admin patient and booking oversight | Evidence gap: A-01 oversight surfaces have not been verified in staging for this report. | Open Patient List/Detail all eight tabs, inspect inquiry/quote/booking context, perform allowed intervention with mandatory reason, and verify audit trail before/after state. | Not reviewed in this pass. | Admin can monitor and intervene only where PRD-supported, with mandatory reason and audit trail. | TBD | Review pending | Silent quote mutation must remain unsupported. |

## A-02 - Provider Management & Onboarding

### Review Notes

- Checked areas: Launch-plan scope; FR-015 provider creation wizard; FR-026 country/calling-code dependency; uploaded screenshot links and user-provided review evidence from 2026-06-08.
- Current state: Admin created `Hairline Test Clinic 1010` with `joachimtrungtuan.work+1010@gmail.com`; provider appears in the provider list. Activation email is present, but the body only exposes button-like text with no usable link fallback, and the admin resend action does not visibly fire. Provider-side password setup and Owner login are not yet tested.
- Code evidence checked: provider creation stores provider lifecycle `status = active` while owner `password_set = false` and activation token remains pending; `NotificationService::sendActivationEmail()` builds and forwards `activation_url`; `ProviderDetails.jsx` renders `Resend Activation Email` with no handler; `AdminNotificationDropdown.jsx` only routes when `onNotificationClick` and `related_url` are present; `PhoneInput` and provider settings country-code selectors show `+code` labels without country names.
- API parity checked for existing provider `test_provider3@clinic.com` / `e4893814-817a-4565-a41f-795ad0ba609e`: API returns city `East Wellington`, country `Canada`, separate `profile_image` and `cover_image`, separate `provider_information.address` and `provider_information.map`, languages `Vietnamese`, `English`, `French`, two staff users, and one uploaded document. This means the observed cover-image issue is a frontend render bug, and the address/map issue is an Admin edit-form parity/exposure issue rather than a backend storage gap.
- Live API parity re-check after creating four invited clinic staff on 2026-06-08 still returns only two legacy `provider_users` rows for `test_provider3@clinic.com` from `provider/get-single-provider`, `provider/get-provider-staff`, and the admin provider-management staff endpoint. This matches the stale Admin/Provider profile-detail staff cards, not the current Provider `Team` table that already shows four active invited staff.
- Live API structure re-check on 2026-06-08 also shows a deeper split than simple list desync: `provider/get-all-providers` now returns the four invited staff emails as separate `providers` records with their own provider IDs, `staff_count = 1`, `owner_full_name = null`, and null clinic location data; one sampled detail call to `provider/get-single-provider?provider_id=1a85fc36-e6a5-44e6-9bc8-95b8f530023a` returns `provider_name = Olivia Carter` and a nested `provider_users` array containing only Olivia as `designation = manager`. Meanwhile the clinic record `e4893814-817a-4565-a41f-795ad0ba609e` still exposes only the old two-member `provider_users` roster, and Admin `Ownership Override` candidates come from that same legacy set.
- Code evidence checked: Admin `/edit-provider/:id` and Provider edit profile share `ProfileSettingsTab` and its child components, so A-02 edit-profile fixes can affect PR-06 provider profile editing and should be regression-tested on both tenants.
- Blocked follow-up checks: provider activation link, password setup form, first Owner login, post-activation login state, and admin resend confirmation cannot be completed until the activation email link/resend wiring issue is fixed. Re-test these items after the P1 activation row is resolved.
- Open verification checkpoint: Activation email/link, password setup, Owner-role login, resend activation, suspension/deactivation, and audit-trail behavior remain unverified. This is not a confirmed bug unless one of those checks fails.
- Review limits: Requires activation email/link, provider password setup, Owner login, resend activation, provider detail/status actions, suspension/deactivation, and audit-trail review.

### Remaining Fixes

| Bug ID | Priority | Flow / Story | Issue | Steps to Reproduce | Actual Outcome | Expected Outcome | Evidence Link | Task Status | Notes |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| A-02-001 | P1 | Provider activation email / resend activation | Activation email has no usable link fallback, the admin resend activation action is not wired, and Admin has no dashboard action to copy the provider activation link for manual recovery. | 1. Create a provider in Admin dashboard.<br>2. Open the activation email.<br>3. Try to click the `Set Your Password` text/button or copy a raw fallback link.<br>4. Return to Admin provider detail and click `Resend Activation Email`.<br>5. Check whether Admin exposes a `Copy activation link` action similar to Provider staff invitation copy-link handling. | The email contains button-like text only; there is no raw fallback URL to copy.<br>Clicking resend in Admin produces no visible change.<br>Admin provider detail does not expose a direct activation-link copy action for support recovery.<br>Provider list/detail can already show lifecycle status `Active` while Owner password setup is still pending, which is technically consistent with FR-015 but ambiguous for admin review. | Activation email should include a real clickable password-setup link and a raw URL fallback.<br>Admin resend should send or queue a new activation email.<br>Admin provider detail should expose a `Copy activation link` action so operations staff can manually resend/share the exact activation URL when email delivery or button rendering fails.<br>Admin UI should distinguish provider lifecycle status from Owner activation/password setup state, using the stored `password_set`/activation-token state where available. | https://s.letweb.net/s/eyk3yj; https://s.letweb.net/s/dz6l13 | Task created (HAIRL-1312) | Blocks activation recovery path when the button link fails.<br>`Active` alone should not be treated as proof that Owner login is ready.<br>The requested admin copy-link behavior should mirror the support value of Provider staff invitation copy-link tooling. |
| A-02-002 | P1 | Provider onboarding notification | New provider onboarding notification does not navigate to User Management / Providers when clicked. | 1. Create a new provider in Admin dashboard.<br>2. Open the notification dropdown.<br>3. Click the `Provider - Onboarding requested` notification for the new provider. | Notification is shown, but clicking it has no visible reaction.<br>It does not route to User Management / Providers. | Notification should deep-link to User Management / Providers.<br>Preferably the relevant provider should be visible, filtered, or highlighted so admin can continue onboarding/review. | https://s.letweb.net/s/evqyp0 | Task created (HAIRL-1313) | Required as part of admin workflow feedback after provider creation. |
| A-02-003 | P2 | Admin support impersonation | `Login as Provider` quick action is visible on provider detail but does not respond when clicked. | 1. Open Admin dashboard provider detail for an active provider.<br>2. Click `Login as Provider` in the Action panel. | No visible response, navigation, modal, read-only impersonation session, or error feedback occurs.<br>Code evidence: the button is rendered without an `onClick` handler, and no clear impersonation API route was found in the current code search. | Per FR-015, `Login as Provider` should be a Super Admin-only support/debugging action.<br>Per FR-009, impersonation should be read-only for troubleshooting and enabled based on admin permissions, with appropriate audit/security handling.<br>If not implemented, the action should be hidden or disabled with clear unavailable-state messaging. | User report on 2026-06-08 | Task created (HAIRL-1314) | Does not block provider activation.<br>Visible dead action degrades admin support workflow and can mislead QA. |
| A-02-004 | P2 | Provider profile media sync | Admin edit can update clinic name, avatar/profile image, and cover image, but Provider portal profile displays the avatar/profile image as the cover image instead of the saved cover. | 1. In Admin provider edit, update clinic name, profile/avatar image, and cover image.<br>2. Save changes.<br>3. Log into the provider portal for that clinic.<br>4. Open Provider Profile. | Provider portal reflects the updated clinic/profile information.<br>The cover area uses the profile/avatar image rather than the distinct cover image uploaded in Admin.<br>Code evidence: backend model/API store and return `profile_image` and `cover_image` separately, but `ProfileBanner.jsx` renders the cover image from `data?.profile_image`. | FR-015 and FR-032 require profile picture/logo and cover image as separate synchronized fields.<br>Provider portal should render cover from `cover_image`/`cover_image_url` and avatar/logo from `profile_image`/`profile_image_url`.<br>Fallback behavior should remain correct when either field is missing. | https://s.letweb.net/s/d1q0p4; https://s.letweb.net/s/g2xorj | Task created (HAIRL-1315) | Cross-tenant sync/display issue between Admin A-02 edit and Provider PR-06 profile display. |
| A-02-005 | P2 | Provider phone capture | Admin provider creation/edit phone fields expose country code as numbers only, store/display poorly formatted international numbers, and reject pasted numbers with common separators. | 1. In Add Provider or Edit Provider, enter provider and clinic phone using country-code dropdown and local number.<br>2. Paste a phone number with spaces or dots into the number field.<br>3. Review the final Review & Create screen and provider detail/list displays. | Country-code selector does not show country names.<br>Pasted numbers containing spaces/dots are rejected by the shared phone input instead of normalized.<br>Review/detail displays raw strings such as `+8491234567890`, making country association and number validity hard to verify.<br>Code evidence: shared `PhoneInput` only updates when the number field matches `/^[0-9]*$/`; options render only the calling code while `optionLabelProp` uses the raw value. | Phone fields should show country name plus calling code during selection.<br>They should accept and normalize pasted phone numbers by stripping common separators before validation.<br>They should validate against the selected country and display normalized international format with readable spacing. | https://s.letweb.net/s/dw7nrw; User report on 2026-06-08 | Task created (HAIRL-1316) | Confirmed in Admin A-02 create/edit provider flow.<br>PR-06 should be regression-tested after the shared phone-control fix, but this row is not evidence that Provider dashboard was directly tested. |
| A-02-006 | P2 | Admin provider edit address / map parity | Admin provider edit Basic info combines clinic address and Google map link into one visible field, while create-provider and backend API keep address and map as separate fields. | 1. Open Add Provider and inspect Clinic Information fields.<br>2. Open Admin provider edit Basic info for `test_provider3@clinic.com`.<br>3. Compare Clinic Address and Google map inputs.<br>4. Save address/map data and inspect API payload. | Create-provider has `information.address` and a separate `information.map` input.<br>Admin edit-provider shows only `information.address` under `Clinic Address / Google map link`.<br>API still stores and returns `provider_information.address` and `provider_information.map` separately.<br>Code evidence: Admin `/edit-provider/:id` shares `ProfileSettingsTab` with Provider edit profile. | Admin edit-provider should expose separate Clinic Address and Google Map Link controls, or otherwise clearly preserve and display both fields without forcing two data concepts into one input.<br>Because the component is shared, Provider edit-profile should be regression-tested after the fix. | https://s.letweb.net/s/e3xvw5 | Task created (HAIRL-1317) | API payload confirms `address = 45 Overlea Boulevard...` and `map = https://maps.google.com/?q=...`.<br>Current bug is Admin edit-form parity/discoverability, with shared-component regression risk. |
| A-02-007 | P2 | Admin provider edit language selector | Admin provider edit `Languages` tab is not consistent with the create-provider language picker: typing in the dropdown does not reliably filter, and the language list appears unsorted/non-alphabetical. | 1. Open Admin dashboard provider edit for `test_provider3@clinic.com`.<br>2. Go to `Languages`.<br>3. Open the language selector.<br>4. Type a language name to filter.<br>5. Compare with the create-provider language selector. | Dropdown opens with a mixed order such as `English`, `Cornish`, `Korean`, `Malayalam`, `Kikuyu; Gikuyu`, etc.<br>Typing does not provide the expected searchable dropdown behavior in the observed UI.<br>Code evidence: create and edit both map the raw `get-languages` API order without alphabetic sorting; Admin edit and Provider edit share `ProfileSettingsTab` / `Language`. | Language selection should consume the centrally managed FR-021/FR-026 language catalog.<br>It should be searchable by typing.<br>It should use a predictable alphabetic or configured display order.<br>It should behave consistently between create-provider and Admin edit-provider flows.<br>Because the component is shared, Provider edit-profile should be regression-tested after the fix. | https://s.letweb.net/s/d4rxy9 | Task created (HAIRL-1318) | Found in Admin A-02 edit provider screen, not from Provider dashboard testing.<br>Shared-component regression risk only for PR-06 until Provider dashboard is directly tested. |
| A-02-008 | P2 | Admin provider edit awards save idempotency | Admin provider edit Awards tab can keep stale pre-save award state after `Save changes`, so a second save without leaving/reloading can append duplicate award records. | 1. Open Admin provider edit for `test_provider3@clinic.com`.<br>2. Go to `Awards`.<br>3. Add a new award, for example `Test 1` / `Test awards` / `2026`.<br>4. Click `Save changes`.<br>5. Stay on the same edit page without refreshing or navigating away.<br>6. Click `Save changes` again, or edit another field and save again.<br>7. Open Admin provider detail Awards section for the same provider.<br>8. Call `provider/get-single-provider?provider_id=e4893814-817a-4565-a41f-795ad0ba609e` and inspect `data.provider_awards`. | Admin edit form does not visibly reload/reconcile Awards state after save.<br>Provider detail shows duplicate awards; reviewer test created `Test 1` once, saved twice without reload, and then saw two `Test 1` awards.<br>Live API check on 2026-06-08 returns `awards_count = 5` for the provider after earlier repeated saves.<br>Existing duplicated awards have different `award_id` values, confirming separate persisted records. | After a successful save, Admin edit state should reconcile with backend response/refetch so newly created awards receive stable IDs in form state.<br>Saving again without changes must be idempotent and must not create duplicate backend records.<br>Existing awards should be updated by stable award ID, not re-created.<br>Admin edit UI, Admin provider detail, and API should represent the same award set after save. | https://s.letweb.net/s/e6jx3k; Live API check on 2026-06-08 | Task created (HAIRL-1319) | Code evidence: backend creates a new award when `awards[index][id]` is missing or cannot be matched. Frontend refetches after save but explicitly avoids updating awards from the refetch, then sets `initialAwards` from current form values; this can leave newly created awards without backend `award_id`, causing the next save to send them as new again.<br>Confirmed in Admin A-02 edit flow; Provider PR-06 should be regression-tested because the edit component is shared. |
| A-02-009 | P1 | Admin provider staff profile/edit mismatch | Admin provider detail `Staff list` and Admin edit-provider `Staff list` stay on a stale two-member roster even after four invited clinic staff are active in Provider `Team`. | 1. Create and accept four invited staff accounts from Provider `Team` for `test_provider3@clinic.com`.<br>2. Open Admin provider detail `Staff list`.<br>3. Open Admin `/edit-provider/:id?tab=3` `Staff list` for the same clinic.<br>4. Compare the rendered staff list with Provider `Team` and the accepted invitation list. | Admin provider detail and Admin edit-provider still show only `Dr. Michael Johnson` and `Mehmet Arslan` instead of the four active invited staff accounts now visible in Provider `Team`.<br>Live API checks to `provider/get-single-provider`, `provider/get-provider-staff`, and `admin/provider-management/providers/{id}/staff` also return only those same two legacy `provider_users` rows.<br>This means Admin staff profile/edit surfaces are not aligned with the accepted invite roster that the provider tenant is actively using. | Admin provider-management staff surfaces should reflect the clinic's current accepted roster consistently with Provider `Team`, especially after invitations have been accepted and the accounts are already active/logging in. | https://s.letweb.net/s/dn55p0; https://s.letweb.net/s/gryyo4; https://s.letweb.net/s/evqq93 | Task created (HAIRL-1320) | Cross-tenant parity issue between older `provider_users`-driven provider detail/profile surfaces and the newer team-management flow.<br>This is now confirmed with both UI evidence and live API responses on 2026-06-08. |
| A-02-010 | P0 | Invited clinic staff materialized as standalone providers | Invited clinic staff are not only missing from the clinic's canonical `provider_users` staff structure; they are being exposed by provider APIs as separate provider records, which breaks the required clinic owner -> team-member hierarchy and causes admin ownership/staff tools to operate on a different dataset from Provider `Team`. | 1. Use Provider `Team` on clinic `e4893814-817a-4565-a41f-795ad0ba609e` to invite and accept four staff accounts.<br>2. Open Admin `Providers` list and locate those invited staff emails.<br>3. Open one invited-staff detail page from Admin `Providers`.<br>4. Open Admin `Ownership Override` for the original clinic.<br>5. Call `provider/get-all-providers?page=1&page_size=20`, `provider/get-single-provider?provider_id=1a85fc36-e6a5-44e6-9bc8-95b8f530023a`, and `admin/provider-management/providers/e4893814-817a-4565-a41f-795ad0ba609e/staff?per_page=20&page=1`. | Admin `Providers` list now includes `Olivia Carter`, `Ethan Brooks`, `Sophie Bennett`, and `Liam Foster` as if they were standalone providers.<br>Live API check on 2026-06-08 confirms the same: `provider/get-all-providers` returns each invited staff email as its own provider row with a unique provider ID, `staff_count = 1`, `owner_full_name = null`, and null clinic location data.<br>Sample detail API for `1a85fc36-e6a5-44e6-9bc8-95b8f530023a` returns a provider object whose `provider_name = Olivia Carter` and whose nested `provider_users` array contains only Olivia as `designation = manager`.<br>At the same time, Admin clinic staff tools (`Staff list`, `Ownership Override`) still query only `ProviderUser::where(provider_id = clinic_id)` and therefore see only the legacy two-member set (`Dr. Michael Johnson`, `Mehmet Arslan`) for clinic `e4893814-817a-4565-a41f-795ad0ba609e`.<br>This means accepted invited staff are no longer just “missing under the clinic”; they appear to have been materialized into separate provider entities that admin can open like independent providers. | Per PRD/schema, `providers` should represent clinics/hospitals, `users` should represent Hairline admins and owner-side accounts, and `provider_users` should represent staff members belonging to a specific clinic/provider.<br>Inviting clinic staff under FR-009 should create or activate team members under the clinic's canonical staff structure, not create new standalone provider records that can appear in the global Admin `Providers` directory.<br>Ownership transfer and admin staff controls must operate on the same canonical clinic team structure that Provider `Team` uses. | https://s.letweb.net/s/g8yy3j; https://s.letweb.net/s/e9yymp; https://s.letweb.net/s/g0oorn; https://s.letweb.net/s/ejnnlw; https://s.letweb.net/s/dkmmoo; https://s.letweb.net/s/gl99r9; https://s.letweb.net/s/emoozj | Task created (HAIRL-1321) | API evidence on 2026-06-08: `get-all-providers` returns invited staff as standalone provider records (`provider_name = personal name`, `staff_count = 1`, `owner_full_name = null`). Sample `get-single-provider` for Olivia returns a provider entity with nested `provider_users = [Olivia as manager]`. Admin `AdminProviderTeamController` ownership/staff flows still operate only on `ProviderUser` under the clinic's provider ID, so they cannot see these new staff records.<br>Required contract from `system-data-schema.md`: `providers` = clinics/hospitals; `provider_users` = staff members working for a provider. Current implementation violates that contract. |
| A-02-011 | P1 | Admin edit-provider `Add New Staff` crash | Admin `Edit Provider -> Staff list` can blank the page when `Add New Staff` is clicked. | 1. Open Admin `Edit Provider` for `test_provider3@clinic.com` and go to `Staff list`.<br>2. Click `Add New Staff`.<br>3. Observe the page state and console. | The staff-edit page turns blank/white instead of opening a staff modal.<br>Console shows `TypeError: h.map is not a function` in the built frontend bundle.<br>Code evidence points to the shared `StaffModal.jsx` role-options mapping: it expects `roleData` to be an array, but `team/roles` returns `{ status, data }`. | Admin edit-provider should open a stable staff modal and read role options from the real API response shape without crashing the entire screen. | https://s.letweb.net/s/dw779k | Task created (HAIRL-1322) | Shared-component crash also affects Provider `Edit Profile -> Staff list` because both tenants use the same `ProfileSettingsTab` and `StaffModal` implementation. |
| A-02-012 | P2 | Provider document preview | Uploaded provider document/image preview is broken in the review/detail display. | 1. Upload a provider document during A-02 wizard.<br>2. Proceed to Review & Create or provider detail view.<br>3. Inspect the Documents section. | Document area shows broken image/file placeholders and hash-like filename fragments instead of a readable document preview/list item. | Uploaded records should display stable file metadata.<br>Use type icon/preview where supported.<br>Expose view/download/remove controls as appropriate for record-keeping. | https://s.letweb.net/s/gxj9wp | Task created (HAIRL-1323) | FR-015 documents are record-keeping and do not block activation.<br>They must remain visible and auditable. |

## A-09a - Content & Treatment Management

### Review Notes

- Checked areas: FR-025 questionnaire catalog/publish/archive/activation/version-history rules, FR-024 admin treatment-create wizard against treatment/package ownership rules and screen field definitions, Admin `Questionnaire Catalog` create/duplicate/archive/set-active behavior, draft-set publish behavior from detail view, archived-set detail recovery path, targeted frontend questionnaire catalog/detail/question-editor/treatment-create code, targeted backend FR-025 route/controller/service definitions, and live API route checks for list/archive/activate on 2026-06-12.
- Current code-state follow-up on 2026-06-22: questionnaire catalog has moved further beyond the earlier mock-only state. Category creation now persists after reload, Context Type Reference remains read-only as required by FR-025, and live Postman MCP verification succeeded for Admin questionnaire-set list, detail, version-history, and audit-log reads on `Sprint1 Inquiry Intake Core A` through `/api/settings/questionnaire-sets/*`.
- Current state: the strongest current questionnaire blocker has narrowed from "catalog CRUD is fully mock/local-only" to two UI-side failures around Screen 2 access. First, `Create New Questionnaire Set` now persists, but success only closes the modal and does not advance the admin into Screen 2 question authoring. Second, `View` or `Edit` can still land on `Questionnaire set not found`, even though the current frontend route and API slice are aligned with the live backend detail endpoint and Postman MCP can fetch the same set successfully. That points to a frontend routing / client-state inconsistency rather than a general backend detail-route outage. Older 2026-06-12 questionnaire rows A-09A-003 through A-09A-008 therefore need to be read as historical findings and re-triaged against the current build rather than taken literally as the current source of truth. The second high-risk mismatch cluster remains the current Admin `Add New Treatment` wizard, which still diverges materially from FR-024 and is also not submittable through a valid FE/BE contract: the wizard only collects `Treatment name`, `Treatment type`, `Description`, a required single thumbnail upload, and an embedded `Packages` step, while FR-024 expects a treatment form with treatment-specific fields such as technique specifications, expected recovery time, typical duration, video, treatment images, educational resources, and status toggle, and also states that packages are provider-owned rather than admin-created in this flow. Targeted code review shows the treatment wizard explicitly posts admin-created packages after treatment creation, sends treatment-type values `hair` / `beard`, uploads `thumbnail`, and treats `video` as optional, while backend validation accepts only treatment types `FUE`, `FUT`, `DHI`, `Sapphire FUE`, `Robotic`, `Other` and requires `video` plus `image[]`.
- Review limits: Requires high-priority review because launch plan risk register identifies A-09a completion as a high Sprint 1 risk.

### Remaining Fixes

| Bug ID | Priority | Flow / Story | Issue | Steps to Reproduce | Actual Outcome | Expected Outcome | Evidence Link | Task Status | Notes |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| A-09A-001 | P1 | Admin Treatment Settings -> Add New Treatment Wizard | The current Admin `Add New Treatment` wizard is materially misaligned with FR-024: it omits multiple required/defined treatment-management fields, enforces a required thumbnail upload not specified by PRD, and embeds admin-created packages even though package ownership belongs to providers. | 1. Open Admin `Settings -> Treatment Settings -> Add New Treatment`.<br>2. Inspect Step 1 `Treatment`, Step 2 `Upload image`, Step 3 `Packages`, and Step 4 `Summary`.<br>3. Compare the available fields and step structure against FR-024 Screen 2 `Admin Treatment Creation/Edit Form` and FR-024 package ownership rules.<br>4. Click `Create Package` from within the wizard and inspect the package modal fields.<br>5. Continue to the summary step and verify which treatment data is actually represented in the wizard flow. | The wizard currently exposes only a narrow treatment form (`Treatment name`, `Select treatment type`, `Description`), a separate `Upload image` step that effectively requires a thumbnail to proceed, a `Packages` step with an admin-side `Create Package` modal, and a summary card that reflects this reduced data model.<br>FR-024 instead defines the admin treatment form as a treatment-centric create/edit form with `Treatment Name`, `Treatment Type`, `Description`, `Technique Specifications`, `Expected Recovery Time`, `Typical Duration`, `Treatment Video`, `Treatment Images`, `Educational Resources`, and `Status`.<br>FR-024 also states that packages are provider-owned and admin may only view provider packages in read-only mode for support; admin should not create packages inside treatment creation.<br>Code evidence matches the observed UI: the wizard state only tracks `treatment_name`, `treatment_type`, `treatment_description`, `thumbnail`, `video`, and `packages`, and on submit it posts to both `/treatment/create-treatment` and `/package/create-package`, meaning the implementation is structurally coupling admin treatment creation with package creation. | Admin treatment creation should follow the FR-024 Screen 2 field model and ownership boundaries: treatment-specific fields and media should be captured in the admin wizard, package management should remain provider-owned, and admin should not be required or invited to create provider-style packages while creating a master treatment. | https://s.letweb.net/s/g5xxvp; https://s.letweb.net/s/e6jjwr; https://s.letweb.net/s/d7kkwq; https://s.letweb.net/s/g8yyxj; https://s.letweb.net/s/e9yy3p | Task created (HAIRL-1331) | PRD source of truth: `fr024-treatment-package-management/prd.md` Screen 2 admin treatment fields and admin/provider ownership rules.<br>Smallest code surface: `main/hairline-frontend/src/pages/teamDashboard/treatments/CreateTreatment.jsx` wizard steps, state shape, validation, and submit path.<br>This is a design/implementation mismatch issue, not just a cosmetic UI difference. |
| A-09A-002 | P1 | Admin Treatment Settings -> Submit New Treatment | Admin can complete the current treatment wizard and click `Submit`, but treatment creation is blocked by an FE/BE contract mismatch, so there is no valid path to create a new treatment from the current UI. | 1. Open Admin `Settings -> Treatment Settings -> Add New Treatment`.<br>2. Enter a treatment name, select `Hair` as treatment type, and enter description.<br>3. Upload the required image and continue through the package and summary steps.<br>4. Click `Submit`.<br>5. Observe the toast and network result for `POST /api/treatment/create-treatment`. | The submit request fails with `422 Unprocessable Content` and the UI shows `The selected treatment type is invalid. (and 2 more errors)`.<br>Although the user selected a treatment type in Step 1, the FE sends values `hair` / `beard`, while backend validation only accepts `FUE`, `FUT`, `DHI`, `Sapphire FUE`, `Robotic`, or `Other`.<br>The FE submit path also appends `thumbnail` and only conditionally appends `video`, while backend validation requires `video` and an `image[]` array.<br>As a result, the current wizard cannot be successfully submitted even when the user completes all visible steps. | The treatment-create wizard must use the same enum values and multipart field contract as the backend, or the backend must accept the UI contract.<br>At minimum, the admin should have one fully valid submission path that persists a new treatment without hidden validation failures. | https://s.letweb.net/s/g0oopn | Task created (HAIRL-1332) | FE evidence: `main/hairline-frontend/src/components/teamComponents/treatmentComponents/TreatmentStep1.jsx` exposes only `hair` / `beard`, and `main/hairline-frontend/src/pages/teamDashboard/treatments/CreateTreatment.jsx` submits `thumbnail` plus optional `video`.<br>BE evidence: `main/hairline-backend/app/Http/Controllers/Packages/TreatmentController.php` validates `treatment_type` against `FUE/FUT/DHI/Sapphire FUE/Robotic/Other` and requires `video` plus `image[]`.<br>This is a runtime product blocker on top of the broader FR-024 design mismatch. |
| A-09A-003 | P1 | Questionnaire Catalog -> Create New Questionnaire Set | Historical create-persistence defect may no longer reproduce in the current admin build, but the original bug task remains open pending cross-check against the existing bug record. | 1. Open Admin `Settings -> App-Settings -> Questionnaire-Catalog`.<br>2. Click `Create New Questionnaire Set`.<br>3. Enter a valid set name, select a valid `Context Type` and `Category`, and click `Save`.<br>4. Refresh the page and search for the new set name. | Latest re-test signal on 2026-06-22 shows the new set is created successfully and survives reload.<br>Current source now calls `createSetApi(body).unwrap()` from the catalog hook and invalidates `QuestionnaireSets`, and live Postman MCP also verified list/detail/version/audit reads for `Sprint1 Inquiry Intake Core A` through the live `/api/settings/questionnaire-sets/*` endpoints.<br>However, this row is not being marked `Resolved` yet because the existing bug task still needs explicit cross-check and closure validation. | Saving a new questionnaire set should create a persisted draft questionnaire-set record, return it from the authoritative questionnaire catalog API, and keep the new row visible after refresh. | User product review 2026-06-22; Postman MCP live verification 2026-06-22 | Task created (HAIRL-1333) | Historical row retained for traceability while the existing bug task is still open.<br>The current follow-up defects after this apparent fix are A-09A-009 (no transition into Screen 2 after create) and A-09A-010 (`View` / `Edit` can still fail with `Questionnaire set not found`). |
| A-09A-004 | P1 | Questionnaire Catalog -> Duplicate Questionnaire Set | Historical duplicate-persistence defect no longer appears FE-local in current source and was reported as working in the latest manual pass, but the original bug task remains open pending cross-check against the existing bug record. | 1. Open Admin `Settings -> App-Settings -> Questionnaire-Catalog`.<br>2. Open the action menu for an existing set.<br>3. Click `Duplicate`.<br>4. Observe the success toast and the copied row.<br>5. Refresh the page and verify the copy is still present. | Current source now calls `duplicateSetApi(id).unwrap()` and invalidates `QuestionnaireSets` instead of cloning local state only.<br>User manual review on 2026-06-22 reported `Duplicate` as working, but a dedicated refresh-persistence check was not separately evidenced in this pass, and the existing bug task has not yet been cross-checked for closure. | Duplicating a questionnaire set should create a persisted draft copy on the backend and keep it in the authoritative questionnaire catalog after refresh. | https://s.letweb.net/s/dn554m; source review 2026-06-22 | Task created (HAIRL-1334) | Historical row retained for traceability while the existing bug task is still open.<br>The old 2026-06-12 FE-local duplicate evidence is stale against the current source tree, but closure should wait for task-level cross-check. |
| A-09A-005 | P1 | Questionnaire Set Detail -> Publish Draft Set | Admin can edit a draft questionnaire set and click `Publish / Activate`, but the published state is only updated locally and disappears after refresh. | 1. Open draft set `Inquiry Follow-up Day 3` from `Questionnaire Catalog`.<br>2. Edit metadata or questions as needed to satisfy local publish readiness.<br>3. Click `Publish / Activate` in the detail screen.<br>4. Use `Back` to return to the catalog and observe the status change.<br>5. Refresh the catalog page and re-check the same set. | The UI shows success toasts and the set can appear updated in the in-session catalog view, but refresh returns the set to its original pre-publish state.<br>Direct code evidence shows the detail screen `Publish / Activate` button is wired only to local `publishSet()` logic that mutates the in-memory mock detail store and returns success without calling a backend publish endpoint.<br>Because the catalog itself is also seeded from local mock data, any apparent publish result is lost on refresh. | Publishing a draft questionnaire set should call the real backend publish lifecycle, persist the new published version, update the authoritative questionnaire catalog, and remain published after refresh. | https://s.letweb.net/s/eykkq1 | Task created (HAIRL-1335) | PRD source of truth: FR-025 requires publish to be a discrete lifecycle step before inquiry activation.<br>Smallest code surfaces: `main/hairline-frontend/src/pages/teamDashboard/settings/QuestionnaireSetDetails.jsx` `Publish / Activate` button and `main/hairline-frontend/src/pages/teamDashboard/settings/questionnaireSetDetails/useQuestionnaireSetDetailsData.js` `publishSet` path. |
| A-09A-006 | P1 | Questionnaire Catalog -> Set as Active for Inquiry on Draft Set | The frontend incorrectly allows a draft set to become `Published + Active Inquiry` immediately in-session when `Set as Active for Inquiry` is clicked, which violates FR-025 lifecycle rules and is not persisted after refresh. | 1. Start from a draft Inquiry-context set such as `Inquiry Follow-up Day 3`.<br>2. Trigger the path that exposes `Set as Active for Inquiry` for that draft set.<br>3. Click `Set as Active for Inquiry`.<br>4. Observe the resulting status and badge in the catalog.<br>5. Refresh the page. | In-session, the FE can immediately change the set from `Draft` to `Published`, apply the `Active Inquiry` badge, and show a success toast without a separate confirmed publish step.<br>After refresh, the change disappears because the catalog returns to seeded mock data.<br>Direct code evidence shows `setInquiryActive(id)` mutates local state and forcibly changes `status` from `draft` to `published` before marking the set inquiry-active, which collapses two PRD lifecycle steps into one FE-only action. | FR-025 requires `Publish` and `Set as Active for Inquiry` to remain separate actions.<br>A draft set must be published first; only a published Inquiry or Multi-Context set may then be designated active for inquiry.<br>The frontend must not auto-promote a draft set directly to `Published + Active Inquiry` from the catalog action. | https://s.letweb.net/s/dz66q4 | Task created (HAIRL-1336) | PRD source of truth: `Set as Active for Inquiry` is available only for published Inquiry / Multi-Context sets, and draft/archived sets must be blocked.<br>Smallest code surface: `main/hairline-frontend/src/pages/teamDashboard/settings/questionnaireCatalog/useQuestionnaireCatalogData.js` `setInquiryActive` path. |
| A-09A-007 | P1 | Questionnaire Catalog -> Archive Questionnaire Set | Admin can archive a questionnaire set in-session, but the archived status is not persisted and disappears after refresh. | 1. Open Admin `Settings -> App-Settings -> Questionnaire-Catalog`.<br>2. From a set row that exposes `Archive`, click the action menu and choose `Archive`.<br>3. Confirm the archive dialog.<br>4. Observe the status tag change in the catalog.<br>5. Refresh the page.<br>6. Re-open the same catalog row and compare its status/action state. | The FE immediately changes the set status to `Archived`, removes inquiry designation locally, and shows a success toast, which makes the archive action appear successful in-session.<br>After refresh, the archived status is lost because the catalog reloads the original seeded mock rows rather than a persisted backend record.<br>Direct code evidence shows `archiveSet(id)` only mutates frontend local state and calls `applyLocalRows(nextRows, \"Questionnaire set archived\")`; it does not call the backend archive endpoint.<br>Live API verification on 2026-06-12 also found both `POST /api/questionnaire-sets/test-id/archive` and `POST /api/questionnaire-sets/test-id/activate-inquiry` returning route-not-found on the deployed backend, even though the codebase defines those FR-025 routes. | Archiving a questionnaire set should persist the status change on the backend, keep the set archived after refresh, and if the set was the active Inquiry questionnaire, remove that designation and leave the inquiry flow explicitly unconfigured until a new set is chosen. | User product review on 2026-06-12; Live API check 2026-06-12: `POST /api/questionnaire-sets/test-id/archive` -> route not found; `POST /api/questionnaire-sets/test-id/activate-inquiry` -> route not found | Task created (HAIRL-1337) | PRD source of truth: FR-025 Screen 1 business rules say sets in use can be archived, archiving an active Inquiry set removes the designation, and `Set as Active for Inquiry` is a separate action available only for published Inquiry/Multi-Context sets.<br>Smallest code surfaces: `main/hairline-frontend/src/pages/teamDashboard/settings/questionnaireCatalog/useQuestionnaireCatalogData.js` `archiveSet`, `main/hairline-frontend/src/pages/teamDashboard/settings/QuestionnaireCatalog.jsx` archive action handler, `main/hairline-backend/app/Http/Controllers/Questionnaire/QuestionnaireSetController.php`, `main/hairline-backend/app/Services/QuestionnaireSetService.php`, and `main/hairline-backend/routes/api.php` FR-025 route group. |
| A-09A-008 | P1 | Archived Questionnaire Set -> Recovery / Reuse Lifecycle | Archived questionnaire sets cannot be brought back into editable/publishable use through the PRD-defined recovery path; the current detail flow leaves archived sets effectively dead-ended. | 1. Open an already archived questionnaire set from the catalog.<br>2. Enter its detail view and inspect available actions.<br>3. Try to return the set to active use from the detail/version-history path.<br>4. Compare available actions with FR-025 recovery rules. | The current detail flow does not expose a PRD-aligned recovery path for archived sets.<br>PRD says archived sets must not be directly designated as active for inquiry; instead, admins should recover them via Version History `Restore as New Draft`, then publish, then separately use `Set as Active for Inquiry` if needed.<br>Current FE detail flow does not provide a restore action, its `Publish / Activate` button is tied only to `publishSet`, and its version-history link goes to a generic catalog audit tab rather than a set-scoped restore flow.<br>Code evidence also shows the frontend mock detail for archived sets only carries non-recovery actions from the catalog seed, while the current backend FR-025 controller/service set exposes archive/duplicate/create-draft/discard-draft but no explicit restore-from-archived-version endpoint matching the PRD's `Restore as New Draft` requirement. | Archived sets should not be directly re-activated while still archived.<br>Instead, FR-025 requires a recoverable lifecycle: Version History must offer `Restore as New Draft`, the restored draft must be publishable, and only then may the published Inquiry/Multi-Context set be designated active via the separate `Set as Active for Inquiry` action. | FR-025 PRD Version History / Inquiry Activation rules; code review 2026-06-12 | Task created (HAIRL-1338) | This is not the same as “archived sets should directly activate.” Direct activation while archived is correctly blocked by PRD and by backend service rules (`Only published sets can be activated for inquiry`). The defect is that the required archived-set recovery path is currently missing/incomplete in the implemented FE/BE lifecycle surfaces. |
| A-09A-009 | P1 | Questionnaire Catalog -> Create Set -> Continue to Question Authoring | Admin can create a questionnaire set successfully, but the success path only closes the modal and does not transition into Screen 2 to continue adding questions. | 1. Open Admin `Settings -> App-Settings -> Questionnaire-Catalog`.<br>2. Click `Create New Questionnaire Set`.<br>3. Fill the required fields and click `Save`.<br>4. Observe the immediate post-save UI state. | The set is created and remains in the catalog after reload, but the modal simply closes and the admin stays on Screen 1.<br>Current source shows the create modal success path only runs `setCreateOpen(false)`, `createForm.resetFields()`, and `setTags([])` after `createSet(...)` succeeds; it never navigates to `/settings/app-settings/questionnaire-catalog/{id}` or any equivalent Screen 2 continuation path. | After successfully creating a set, the admin should land on Screen 2 or an equivalent next-step flow to continue question authoring immediately, matching the FR-025 set-creation workflow. | User product review 2026-06-22; source review 2026-06-22 | Recorded only | PRD source of truth: FR-025 Workflow 1 and Screen 2 describe create -> manage contents as one continuous admin flow.<br>Smallest code surface: `main/hairline-frontend/src/pages/teamDashboard/settings/QuestionnaireCatalog.jsx` create modal `onFinish` success branch. |
| A-09A-010 | P0 | Questionnaire Catalog -> View / Edit Questionnaire Set | Persisted questionnaire-set rows can still open a `Questionnaire set not found` screen from `View` or `Edit`, blocking Screen 2 access. | 1. Open Admin `Settings -> App-Settings -> Questionnaire-Catalog`.<br>2. Create a questionnaire set or select an existing persisted row from the catalog.<br>3. Click the set name, `View`, or `Edit`.<br>4. Observe the detail page result. | The UI can navigate to the detail route and show `Questionnaire set not found.` instead of loading Screen 2.<br>Current frontend routing and API wiring are aligned with the backend contract: catalog actions navigate to `/settings/app-settings/questionnaire-catalog/${row.id}`, the RTK query calls `GET /settings/questionnaire-sets/{id}`, and the backend `show()` action returns `QuestionnaireSetDetailResource` for found IDs.<br>Live Postman MCP on 2026-06-22 successfully fetched list, detail, version history, and audit logs for `Sprint1 Inquiry Intake Core A`, so the current evidence points to a frontend routing / client-state inconsistency or wrong row-id binding in the UI flow rather than a general backend detail-endpoint outage. | Clicking a set name or `View` / `Edit` should open Screen 2 with the set metadata, ordered questions, usage locations, and version-history entry points. | https://s.letweb.net/s/gl99x7; Postman MCP live verification 2026-06-22 | Recorded only | Smallest code surfaces: `main/hairline-frontend/src/pages/teamDashboard/settings/QuestionnaireCatalog.jsx`, `main/hairline-frontend/src/features/hairlineTeam/settings/questionnaireCatalogApiSlice.jsx`, `main/hairline-backend/app/Http/Controllers/Questionnaire/QuestionnaireSetController.php`, and `main/hairline-backend/app/Services/QuestionnaireSetService.php`.<br>This is the first questionnaire defect that should be manually re-tested after create persistence because it blocks all deeper Screen 2 authoring and publish checks. |
|  | P1 | Questionnaire Set Version History & Audit Trail data fidelity | Scout signal: version-history and audit endpoints are live, but the current UI may hide meaningful change detail required by FR-025 Screen 7. | 1. Open a questionnaire set that has create/edit/publish activity.<br>2. Compare the Version History and Audit Trail tabs in the UI against the backend payload for the same set.<br>3. Check whether change details remain specific enough to explain what changed. | Postman MCP verified that versions and audit-log arrays are returned for `Sprint1 Inquiry Intake Core A`, but the current frontend mapping reduces richer audit metadata such as `before` / `after` payloads to generic strings like `Metadata updated`.<br>This means the tabs may look populated while still under-reporting the change detail expected by Screen 7. | Screen 7 should expose meaningful version summary and audit detail for each admin action on the set, not just a minimal populated table. | Postman MCP live verification 2026-06-22; source review 2026-06-22 | Scout flagged | Good narrow follow-up after fixing Screen 2 access because it tests data correctness rather than basic endpoint availability. |
|  | P0 | Active inquiry questionnaire end-to-end propagation | Scout signal: A-09a is in a mixed state where questionnaire catalog endpoints exist, but detail publish/activate and downstream inquiry consumption still do not share one source of truth. | 1. Publish or switch the active inquiry set from Admin `Questionnaire Catalog` or detail view.<br>2. Start a new patient inquiry and confirm whether the changed set is actually shown.<br>3. Submit the inquiry and inspect provider or admin review output for the same answers and alerts. | Backend questionnaire-set routes and catalog list/create wiring now exist, but detail publish remains FE-local in places and patient or provider inquiry still depends on hardcoded or static questionnaire structures outside FR-025 lifecycle control.<br>This means A-09a can look partially ready while the actual inquiry flow still ignores the supposedly active set. | A-09a should be able to publish one authoritative active inquiry set that the patient inquiry and provider or admin review flow actually consume. | Code review 2026-06-18 | Scout flagged | This is the highest-risk A-09a feature for Sprint 1 because downstream inquiry QA depends on it. |
|  | P1 | Questionnaire, legal content, and settings configuration | Evidence gap: A-09a content/settings readiness has not been verified in staging for this report. | Manage questionnaire catalog and active set, publish legal documents, verify acceptance coverage, update OTP config, country/calling-code list, cancellation/deletion reasons, and OTP email template. | Not reviewed in this pass. | Active questionnaire/settings/legal content support Sprint 1 inquiry, auth, account lifecycle, and compliance flows. | TBD | Review pending | Treat as hard readiness check due to risk-register callout. |

## S-01 - Head Scan Media Processing Service

### Review Notes

- Checked areas: Launch-plan scope only.
- Current state: Product behavior not checked in this pass.
- Review limits: Requires media-processing API/service evidence tied to patient upload flow.

### Remaining Fixes

| Bug ID | Priority | Flow / Story | Issue | Steps to Reproduce | Actual Outcome | Expected Outcome | Evidence Link | Task Status | Notes |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
|  | P1 | Media intake and alert classification | Evidence gap: S-01 processing behavior has not been verified in staging for this report. | Upload head-scan photo set, inspect processing job, verify normalized payload, media URI, and alert classification result. | Not reviewed in this pass. | Processing handles concurrent intake within target latency and returns trusted media/classification output. | TBD | Review pending | Required for provider review and downstream clinical record. |

## S-02 - Payment Processing Service

### Review Notes

- Checked areas: Launch-plan scope only.
- Current state: Product behavior not checked in this pass.
- Review limits: Requires Stripe deposit charge, webhook, status, audit, and refund-eligibility service review.

### Remaining Fixes

| Bug ID | Priority | Flow / Story | Issue | Steps to Reproduce | Actual Outcome | Expected Outcome | Evidence Link | Task Status | Notes |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
|  | P0 | Deposit success state synchronization | Scout signal: the current payment flow is split between Stripe payment-intent handling, a commented-out confirm endpoint, and a legacy simulated deposit path. | 1. Accept a quote and create the deposit intent.<br>2. Complete the payment sheet successfully.<br>3. Inspect booking, payment, and slot state in patient, provider, admin, and backend records. | Patient app currently stops after payment-sheet success toast, backend confirm-success routing is commented out, and legacy `pay-deposit` still simulates success while leaving TODOs for payment record creation and downstream updates.<br>This creates a concrete risk of inconsistent booking or payment state and missing slot-blocking or notifications after a nominally successful deposit. | One confirmed payment-success path should persist payment records, update booking state, block the slot, and trigger the expected downstream visibility consistently. | Code review 2026-06-18 | Scout flagged | Highest-priority shared-service check after questionnaire dependency because it can corrupt booking and treatment fixtures. |
|  | P0 | Core deposit infrastructure | Evidence gap: S-02 deposit infrastructure has not been verified in staging for this report. | Pay deposit through Stripe, observe pending/completed/failed status, trigger webhook handling, inspect booking/payment state update, and verify immutable audit entries. | Not reviewed in this pass. | Deposit charge, payment status, webhook update, audit log, and refund eligibility calculation work for Sprint 1. | TBD | Review pending | Refund UI is not Sprint 1 scope. |

## S-05 - Media Storage Service

### Review Notes

- Checked areas: Launch-plan scope only.
- Current state: Product behavior not checked in this pass.
- Review limits: Requires encrypted bucket, signed URL, access-control, and lifecycle-policy evidence.

### Remaining Fixes

| Bug ID | Priority | Flow / Story | Issue | Steps to Reproduce | Actual Outcome | Expected Outcome | Evidence Link | Task Status | Notes |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
|  | P1 | Media access control vs signed URL contract | Scout signal: media can still be served through a public file-view route rather than only through controlled signed access. | 1. Upload inquiry or head-scan media.<br>2. Resolve the returned media URL.<br>3. Attempt direct access without the intended authenticated tenant context. | Current backend can resolve media to a public `/media/file/{id}` or `/storage/{path}` URL, and the file-view route serves public files without signed-token or auth checks.<br>This is a concrete risk against the signed-URL access-control expectation for Sprint 1 media. | Media access should be limited to authorized signed URLs or another controlled access mechanism, with direct unauthorized access blocked. | Code review 2026-06-18 | Scout flagged | Relevant for inquiry photos and later treatment or head-scan evidence. |
|  | P1 | Encrypted media storage | Evidence gap: S-05 media storage behavior has not been verified in staging for this report. | Upload media, inspect storage target, test signed URL access, test unauthorized access denial, and confirm lifecycle policy configuration. | Not reviewed in this pass. | Media is encrypted, access is controlled through signed URLs, and retention/lifecycle policies are configured for later compliance alignment. | TBD | Review pending | Supports FR-002 media storage layer. |

---

# 3. Not For This Sprint

| Item | Why It Is Not In This Sprint | Follow-Up Notes |
|---|---|---|
| Aftercare activation journey | Deferred to Sprint 2 with P-05, PR-04, A-03, A-09b, and related configuration. | Keep out of Sprint 1 fix backlog unless a Sprint 1 treatment-completion dependency prevents Sprint 2 activation. |
| Payment installments, multi-currency display, receipt/reference UX, refund request UI | Deferred to Sprint 2 under P-03b and payment-rule configuration. | Sprint 1 validates core base-currency deposit only. |
| Final-balance patient payment path | Deferred to Sprint 2. | Sprint 1 PR-03 uses a fully-paid staging booking fixture for treatment execution verification. |
| Travel and logistics | Patient/provider workflows and S-04 gateway are Sprint 2; admin embedded oversight is Sprint 4. | Do not treat missing travel surfaces as Sprint 1 bugs. |
| Full notification delivery-channel verification | Deferred to Sprint 3 under S-03. | Sprint 1 still needs visible event/state recording where relevant. |
| Affiliate/partner workflows | Later launch scope, not assigned to Sprint 1. | FR-018 is launch-scope but not Sprint 1 module scope. |
| True 3D head-scan viewing | Out of launch scope unless separately approved. | Sprint 1 validates V1 standardized 2D photo sets only. |
