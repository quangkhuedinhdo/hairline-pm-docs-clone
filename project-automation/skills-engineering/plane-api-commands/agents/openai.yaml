interface:
  display_name: "Plane API Commands"
  short_description: "Refresh Plane values and create or update Hairline work items"
  default_prompt: "Run Plane API operations for refreshing metadata, creating work items from task files, or updating existing work items."
