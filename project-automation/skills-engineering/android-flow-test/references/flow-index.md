# Android Flow Index

Registered flows for the Hairline app. Managed by the `android-flow-register` skill.

When matching a user's flow request, check both **Flow Name** and **Aliases** columns. Present all candidates and ask the user to confirm before loading any flow file.

> **This index starts empty.** Run `android-flow-register` to record and register a flow. Flow definition files are stored in `references/flows/`.

| Flow Name | Aliases | File | Status | Last Recorded |
|-----------|---------|------|--------|---------------|

_No flows registered yet._
